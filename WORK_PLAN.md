# NewMeans 리디자인 작업 계획

기준 문서: `DESIGN_PLAN.md` (2026-09-12). 이 문서는 그 계획을 실제 작업 단위로 쪼갠 실행 계획이다. 디자인 결정 자체는 `DESIGN_PLAN.md`에 기록하고, 여기에는 순서·파일·검증 방법·결정 시점만 적는다.

상태: 단계 5 진행 중 (2026-09-13). 홈·공용 스타일·404 적용 완료, 제품 페이지 재구축과 검증 마무리 남음.

## 0. 공개 저장소 운영 규칙

이 저장소는 공개이고, `main`에 올라간 파일은 전부 `newmeans.github.io/<경로>`로 공개된다. 그래서 다음 규칙을 지킨다.

| 무엇 | 어디에 | Git |
|---|---|---|
| 외부 사이트 캡처·HTML·소스·조사 메모 | `_local/refs/` | 무시됨 (`.gitignore`) |
| 시안 비교 보드, 실험용 페이지 | `_local/drafts/` | 무시됨 |
| 작업 메모, 측정 원본 로그 | `_local/notes/` | 무시됨 |
| 요구사항, 디자인 결정, 자체 제작물, 검증 결과 요약 | `DESIGN_PLAN.md`, `WORK_PLAN.md`, 사이트 파일 | 추적 |

- 외부 레퍼런스의 사이트명·URL은 코드 주석, 에셋명, 커밋 메시지, PR 제목·본문, 이 문서에 쓰지 않는다. 결정만 적는다 ("모서리 반경을 줄임"처럼).
- 시안 비교는 `_local/drafts/`의 로컬 페이지나 비공개 링크로 공유하고, 확정된 한 방향만 사이트 파일에 반영한다. 드래프트 페이지를 `main`에 머지하지 않는다.
- Jekyll 기본 설정이라 `_`로 시작하는 폴더는 Pages에 배포되지 않는다. `_local/`은 Git에서도 배포에서도 빠진다.
- 커밋 전 `git status --short`로 추적 대상만 올라가는지 본다. `.gitignore`는 앞으로의 추가만 막고 이미 커밋된 이력은 지우지 않는다.

## 1. 출발점 — 지금 코드에서 이어 쓰는 것

- 사이트는 빌드 없는 정적 HTML/CSS/JS이고 3D 라이브러리가 없다. 페이지: `index.html`, `typer.html`, `dopamine.html`. 공통: `base.css`, `i18n.js`, `experience.js`, `keyboard.js`.
- 토끼와 워드마크는 이미 25개 픽셀 조각(`assets/brand/logo-pieces/p1`–`p26`, p6 없음)으로 분리되어 있고, `logo.js`의 `PARTS` 표가 4268×2134 좌표계에서 각 조각의 위치와 크기를 갖고 있다. 이 좌표계를 3D 배치의 기준으로 그대로 쓴다.
- 조각 역할:
  - 귀: p4·p1 (왼쪽 `/` `)`), p5·p2 (오른쪽)
  - 얼굴: p9 `(`, p12·p13 눈, p14 `^`, p15 볼 `*`, p10 `)`
  - 몸·꼬리: p8 `⌒`, p11 `)`, p16 `ɔ`, p17·p18 앞발 `_`
  - 머리 위 장식: p3 `°`, p7 `*`
  - 팀 이름 8글자 (왼쪽부터): p19 N, p23 e, p21 w, p20 M, p24 e, p25 a, p22 n, p26 s
- 총 25개 형상(토끼 17 + 글자 8)을 개별 3D 요소로 만든다. `DESIGN_PLAN.md` §6의 "토끼 각 요소와 여덟 글자를 개별 3D 요소로"에 그대로 대응한다.
- `logo.js`의 2D 반응(시선·쓰다듬기)은 WebGL을 쓸 수 없을 때의 폴백으로 남긴다.
- `keyboard.js`(Typer 키보드·타건음)와 `i18n.js`는 구조를 바꾸지 않고 재사용한다.
- 카피 작업은 `.claude/skills/newmeans-copy/SKILL.md`의 보이스 규칙과 `.claude/product-marketing.md`의 사실만 쓴다.

## 2. 기술 방향 (제안 — 단계 2의 측정으로 확정)

| 항목 | 제안 | 이유 |
|---|---|---|
| 렌더러 | three.js, 버전 고정, ES module + importmap (jsdelivr) | 폰트도 이미 jsdelivr CDN. 빌드 도구 없이 유지. 외부 런타임 의존을 없애고 싶으면 `vendor/three/`에 같은 버전을 복사 |
| 지오메트리 | 픽셀 조각 PNG → 외곽선 추출 → `assets/3d/outlines.json` (수 KB) → 런타임 `ExtrudeGeometry`(두께 + 베벨) | 모델 다운로드 0. 글자를 폰트로 다시 치지 않고 워드마크 윤곽 그대로 (§4 규칙). 재질 변경 시 지오메트리 재생성 없음 |
| 외곽선 추출 도구 | `tools/trace_outlines.py` (Python 3.12 + Pillow, 로컬 확인 완료) | 자체 제작물이라 추적 가능. 조각 원본은 이미 저장소에 있음 |
| 조명·환경 | `RoomEnvironment` + PMREM (절차적) | HDR 다운로드 0. 금속·투명 재질의 반사가 보이는 최소 조명 |
| 접지 그림자 | 흐린 타원 텍스처 1장 또는 작은 평면 | 실시간 그림자 맵 없음 (§11) |
| 재질 | `MeshPhysicalMaterial` 프리셋 3종 (Soft Mint / Clear Mint / Milky White) | Clear Mint는 transmission 사용 → 비용 별도 측정, 모바일 기본에서 제외 검토 |
| 렌더 루프 | 온디맨드. 포인터·스크롤·조작·복귀 애니메이션이 있을 때만 프레임 갱신. `visibilitychange`·`IntersectionObserver`로 정지 | §11 "정지 상태 프레임 없음" |
| 해상도 | DPR 데스크톱 1.5, 모바일 1.0 상한 | §11 예산 |
| 접근성 계층 | 캔버스 `aria-hidden`. 카피와 `NewMeans`는 DOM 텍스트. 토끼·글자 8개·화살표·다시 뽑기는 투명 `button` 오버레이로 Tab/Enter/Space 대응 | §7 모바일·키보드 |
| 초기 상태·리빌 | 첫 페인트는 `logo.js`의 2D 조각(완전 평면·원색). 캔버스는 뒤에서 준비하고 첫 프레임을 정사영 카메라·무조명(`MeshBasicMaterial`)으로 같은 픽셀 박스에 맞춰 렌더한 뒤 교체. 리빌 시 원근·조명·재질·그림자로 전환 | 사용자 요구: 처음엔 완전 평면·브랜드색, 상호작용으로 3D가 드러남. 초기 상태와 폴백이 같아져 첫 콘텐츠 표시도 빨라짐 |
| 폴백 | WebGL 생성 실패 → 같은 자리에 `logo.js` 2D 구성. `prefers-reduced-motion` → 3D 유지, 이동량·회전 축소, 첫 방문 제스처 생략 | §11 |
| 상태 유지 | 다시 뽑기 결과를 `sessionStorage`에 저장. 새로고침·제품 페이지 이동 시 같은 토끼 | §9 |

새 파일 (단계 5에서 생성): `hero3d.js`, `home.css`(`index.html`의 인라인 스타일 분리), `assets/3d/outlines.json`, `tools/trace_outlines.py`.

## 3. 단계별 작업

`DESIGN_PLAN.md` §12의 협의 순서를 그대로 따른다. 각 단계 끝에 사용자 결정이 있고, 결정 전에는 다음 단계의 구현을 시작하지 않는다. 결정은 `DESIGN_PLAN.md` §2 표를 갱신해 기록한다.

### 단계 0 — 준비 (완료)

- [x] `.gitignore`에 `_local/` 추가, `_local/{refs,drafts,notes}/` 생성
- [x] 이 문서 작성
- [x] 브랜치: 현재 `dev/refine-product-experience`(초안 PR #5)에서 그대로 이어간다. 지금 첫 화면은 유지 대상이 아니므로 통째로 교체해도 된다.

### 단계 1 — 정지 상태 디자인 결정 (§12-1)

목표: 같은 구도에서 폰트 1안/2안, 카피 후보, 기본 팔레트를 나란히 보고 고른다.

작업:
1. `_local/drafts/static-board.html` — §3의 데스크톱 구도(좌상단 카피 / 우하단 토끼 + 팀 이름 / 모서리 표시)를 1440×900, 1280×720(짧은 화면), 390×844로 렌더. 토끼와 팀 이름은 현재 픽셀 조각을 목표 위치에 완전 평면·원색으로 놓는다(초기 상태 그대로).
2. 비교 축: 폰트 1안(Nunito Sans + Wanted Sans) × 2안(Manrope + SUIT), 카피 1안·2안(3안은 §5 충돌 참고), 팔레트(`#FCFBF7` 배경 / `#514D48` 카피 / `#69645B` 보조), 한글 한 줄 설명 유무, 좌하단 문구.
3. 각 조합의 텍스트 대비를 표로 첨부 (WCAG AA 기준).
4. 스크린샷을 모아 사용자에게 전달. 폰트는 비교용으로만 로드하고 사이트 파일에는 넣지 않는다.

사용자 결정: 폰트, 주요 카피, 팔레트, 한글 설명 유무, 좌하단 문구(`Seoul, Korea` 허용 확인됨), 첫 방문 기본 재질(단계 2와 함께 봐도 됨).

완료 조건: `DESIGN_PLAN.md` §2·§4·§5의 미확정 항목이 확정으로 바뀜.

산출물 (2026-09-12, 결정 대기):
- `_local/drafts/board.html` — 로컬 비교 보드 (저장소 루트에서 정적 서버를 띄워 연다)
- `_local/drafts/hero.html?font=1|2&copy=1|2|3&sub=0|1&corner=seoul|indie` — 단일 시안
- `_local/drafts/shots/contact-desktop.png`, `contact-variants.png`, `contact-mobile.png` — 라벨 붙은 비교 시트. 개별 컷은 같은 폴더
- `_local/notes/phase1-contrast.md` — 팔레트 대비 측정

2차 (2026-09-12, 사용자 피드백 "카피는 '조금 더 즐거운 일상' 뉘앙스, 폰트가 평이하고 생성형 느낌" 반영, 결정 대기):
- `_local/drafts/hero2.html?d=1|2|3|4&copy=1|2|3|4&sub=1|2|0&ko=0|1&full=0|1` — 방향 A–D, 새 카피, 한글 헤드라인, 전체 페이지(벤토·푸터)
- `_local/drafts/shots2/contact2-directions.png`, `contact2-copy.png`, `contact2-mobile.png`, `full-d1.png`, `full-d2.png`
- `_local/tools/shoot.mjs` — 로컬 Playwright 캡처 스크립트 (`node _local/tools/shoot.mjs jobs.json outdir`)
- 참고 갤러리 관찰은 `_local/refs/notes.md`에만 둔다. 플랜에는 사이트명 없이 결정만 옮겼다.

### 단계 1.5 — 디자인 시스템 v1 (2026-09-12 추가)

사용자 피드백: 2차 벤토 초안은 색·서체 사용에 규칙이 없고, Typer 지표가 홈에 노출됐다. 색부터 시스템으로 다시 잡는다. 사용자가 지정한 안티슬롭 프론트엔드 스킬(`design-taste-frontend`, 사용자 전역에 설치, 저장소 밖)의 규칙을 적용 기준에 추가했다.

산출물:
- `DESIGN_SYSTEM.md` — 디자인 읽기·다이얼, OKLCH 램프(중립·민트·코랄), 역할 토큰, 대비 측정, 서체 역할, 형태·간격, 컴포넌트, 모션·콘텐츠 규칙, 미결정 3건
- `_local/drafts/tokens.css` — 토큰 CSS (단계 5에서 `base.css`로 이식)
- `_local/drafts/system.html`, `_local/drafts/home3.html` — 시스템 시트와 적용 시안. 캡처는 `_local/drafts/shots3/`
- `_local/tools/palette.py` — 램프·대비 생성 스크립트

결정(2026-09-12): v1.1로 진행(중립 밝게, 아이브로 없음, 버튼 정렬 보정). 다크 모드는 라이트 고정, `Seoul, Korea`는 히어로 모서리 유지, 강조어는 색. 시스템 생성은 `_local/tools/gen_system.py`.

완료 조건: 승인된 토큰이 `DESIGN_PLAN.md` §4의 기준이 되고, 이후 모든 시안은 `tokens.css`만 쓴다.

### 단계 2 — 3D 아트 방향 (§12-2)

목표: 토끼와 글자의 두께·모서리·기본 재질을 실제 렌더로 보고 고른다. 성능 예산의 첫 측정도 여기서 한다.

작업:
1. `tools/trace_outlines.py` — 25개 조각의 알파 마스크에서 외곽선 폴리곤을 뽑아 `assets/3d/outlines.json`으로 저장 (id, 역할, 4268×2134 좌표, 외곽선 점 목록). 픽셀 계단을 그대로 둔 버전과 반 픽셀 단위로 둥글린 버전 둘 다 생성.
2. `_local/drafts/3d-board.html` — three.js로 토끼 + 팀 이름을 정면·측면·약간 기울인 시점으로 렌더. 변수: 두께(글자 높이의 약 8% / 12% / 16%), 베벨 크기·단 수, 계단 유지 vs 둥글림, 재질 3종.
3. 측정: `renderer.info.render.triangles`, 전송량(three.js + outlines.json + 텍스처), 재질별 프레임 시간(특히 transmission), 데스크톱·모바일 DPR 상한에서의 화질.
4. 액세서리(작은 리본·헤드폰) 형태 2–3개를 같은 보드에서 크기 한계와 함께 제시.
5. 리빌 전/후를 같은 보드에서 비교: 평면 상태(정사영·무조명, 2D 조각과 픽셀 단위로 겹쳐 확인)와 드러난 상태(원근·조명·재질). 첫 프레임이 2D 조각과 어긋나면 교체 순간이 보이므로 오차를 측정한다.

사용자 결정: 두께·모서리, 계단/둥글림, 기본 재질, 변주 팔레트(어울리는 조합 6개 안팎), 액세서리 종류.

완료 조건: 정면·측면·움직임에서 원래 토끼로 읽힘. 삼각형 수·전송량이 §11 예산 안이거나 조정 계획이 있음.

산출물 (2026-09-13, 결정 대기):
- `_local/tools/trace_outlines.py` → `_local/drafts/3d/outlines.json` (steps·smooth 두 변형, 구멍 포함)
- `_local/drafts/3d/board.html?view=flat|front|side|tilt&mat=flat|soft|clear|milky&t=8|12|16&bevel=0.15|0.3|0.45&variant=smooth|steps` — three.js 보드
- `_local/drafts/3d/shots/sheet-*.png` — 정합·두께·재질·계단/베벨 비교 시트, 측정은 `_local/notes/phase2-3d.md`
- 결과: smooth 13,854 삼각형(예산 20,000 이내), steps 141,582(제외). 평면 상태는 2D 조각과 정합. 지오메트리 생성 9ms.
- 결정(2026-09-13): 직각 압출(베벨 0). 정면 = 평면. smooth 3,400 삼각형, steps 35,334(제외). 시트는 `sheet-sharp.png`. 두께·기본 재질은 결정 대기.
- 재질 방식(2026-09-13): 앞뒤면 무조명 원색 + 옆면 조명(`cap=flat`). 정면 색 `#85C6A8` 정확 일치. 두께는 작업값 10%로 단계 3 진행.

### 단계 3 — 행동 규칙 (§12-3)

목표: §7 표의 반응 순서와 §6 모션 강도를 프로토타입에서 맞춘다.

작업:
1. 단계 2 보드에 상호작용 추가: 포인터 시선(토끼 전체 ±5°/±3°), 귀·눈·코 추가 회전, 카피 단어 호버·누름 → 토끼 시선 이동, 토끼 누름 → 가까운 글자 1–2개 눌림, 글자 누름 → 토끼가 그 글자를 봄, 화살표 접근·클릭.
2. 복귀 시간 180–350ms 범위에서 값 후보 2개.
3. 첫 방문 1회 제스처(토끼가 카피를 보고 가까운 글자를 건드림) 구현, 사용자 입력 시 취소.
4. 터치: 첫 탭 즉시 반응. 키보드: 오버레이 버튼으로 Tab/Enter/Space. 움직임 줄이기: 이동량 축소 모드.
5. 390px 폭에서 글자 8개의 터치 영역이 44px 미만이면 2글자씩 묶는 규칙 적용.
6. 리빌 트리거 후보 비교: (a) 포인터 접근 시 아주 작은 기울임으로 예고하고 첫 누름에서 완전히 드러남, (b) 첫 누름에서만 드러남. 터치는 첫 탭. 드러나기 전 반응은 `logo.js` 방식의 2D 유지. 움직임 줄이기에서는 기울임 없이 조명·그림자만 켜는 안 검토.

사용자 결정: 리빌 트리거, 모션 강도 최종값, 복귀 시간, 첫 방문 제스처 채택 여부(채택해도 3D는 사용자 상호작용 뒤에만 드러남).

완료 조건: 설명 없이 조작 가능한 요소를 찾을 수 있는지 사용자 확인. 호버 없이 클릭만으로도 반응이 완결됨.

산출물 (2026-09-13, 결정 대기):
- `_local/drafts/3d/proto.js` + `_local/tools/gen_proto.py` → `_local/drafts/3d/proto.html?trigger=press|hover&t=10&reduce=1` — 히어로 상호작용 프로토타입. 첫 페인트는 2D 조각(평면·원색), 캔버스는 뒤에서 FOV 3°(정사영에 가까움)로 같은 자리에 준비. 첫 누름(글자·토끼·다시 뽑기)에서 FOV 3→24°, 카메라 고도 0→6°, 구성 기울임(-5°/±7°), 접지 그림자 페이드로 700ms 리빌. 움직임 줄이기에서는 즉시 전환·진폭 0.35배.
- 반응: 포인터 시선(토끼 ±5°/±3°, 구성 ±2.5°/±1.5°, 눈·코 이동, 귀 기울임), 글자 누름(깊이 0.6t 눌림 + 2.5° 회전, 110ms 눌림 / 300ms 복귀, 토끼가 그 글자를 650ms 바라봄), 토끼 누름(눈 접힘, 귀 -8°/+6°, 볼 별 1.18배, 가까운 글자 w·M 순차 눌림), 카피 강조어 호버 시 토끼가 단어를 봄, 클릭 시 단어 눌림 + 토끼 반응, 다시 뽑기(앞면·옆면 색 조합 3종 순환, 내비 색 불변).
- 접근성: 토끼 1 + 글자 8(좁은 화면은 2글자씩 4)의 투명 버튼, Tab/Enter/Space, `role=status`로 반응 안내. 캔버스 `aria-hidden`.
- 측정: 3,402 삼각형. 리빌 후 정지 시 프레임 렌더 0(155프레임에서 멈춤 확인). 캡처는 `_local/drafts/3d/shots/proto-*.png`.

v2 (2026-09-13, 사용자 지시 반영):
- 리빌 트리거 = 첫 스크롤. 맨 위에서 wheel/touch를 가로채 약 1,100px 동안 페이지를 고정. 0–55%는 smoothstep으로 카메라 하강(-12°), 55–90%는 멈춤 구간(-12 → -14.5°로 느린 드리프트), 100%에서 +4°로 복귀해 3D 유지. 스크롤 입력은 프레임당 10% 관성으로 따라온다. 완료 전 역방향 스크롤은 되돌림. 측정: 250px → 진행 0.23, 550px → 0.5(바닥), 1000px → 0.91, 1150px → 완료·이후 wheel 400 → scrollY 400. 누름·키보드는 같은 경로를 900ms로 재생. 튜닝 파라미터 `?dist=&hold=&dip=&rest=`.
- 두께 = 조각별 획 폭(정사각 단면). 글자 120, 토끼 48 안팎. 앞면은 무조명 원색.
- 상단바 브랜드 = 잉크 워드마크. 제품 영역 = Typer 데스크 장면(실제 키보드·스위치 7종 소리) + 도파민대학교 카드 더미 장면 + 스튜디오·연락 한 줄. 푸터 = 한 줄 + 잠든 토끼.
- 산출물: `_local/drafts/3d/proto.html`(v2), `_local/tools/gen_proto.py`, 캡처 `v2-*.png`, 시트 `sheet-scroll.jpg`, 전체 페이지 `v2-full.jpg` / `v2-mobile-full.jpg`.

v3 (2026-09-13, 사용자 지시 반영):
- 기존 사이트(main `logo.js`)의 규칙을 3D로 이식: 포인터 추종은 위치 이동(얼굴 1.3·귀 0.6·몸 0.45·글자 0.12 × 폭의 3%, 스프링 k .16 / 감쇠 .74), 글자 드래그(포인터 캡처, 놓으면 스프링 복귀), 안절부절(눈 동그란 링 메시로 교체, 발 구르기 |sin 13t|·32°, 꼬리 sin 26t·30°, 얼굴 흔들림, 땀 DOM), poke(글자 튀어오름). 토끼 클릭·강조어 클릭·짧은 탭 = poke.
- 리빌 스크롤 동안 카피가 한 줄 26px로 축소, `.brandwrap`이 translate+scale로 화면 중앙·최대 1.5배(폭 82%·높이 50% 이내). 히트 영역·땀도 같이 이동.
- 다시 뽑기 = 틴트 알약(아이콘 + 글자). 렌더 루프는 포인터 이동 후 1.2초, 스프링·드래그·안절부절·트윈 중에만.
- 캡처 `v4-*.png`, 시트 `sheet-v4.jpg`.

v4 · 2D 전환 (2026-09-13, 사용자 결정: 3D는 애니메이션과 겹치면 렉, 2D로):
- 3D·스크롤 연출(카피 축소, 토끼 확대)은 보류. `_local/drafts/3d/`는 참고용으로 남긴다.
- `_local/drafts/hero2d.js` + `_local/tools/gen_home2d.py` → `_local/drafts/home2d.html`. 토끼는 마스크 방식(`mask-image` + 배경색)이라 어떤 색으로도 칠해진다. main의 규칙(시선·드래그·안절부절·poke) 그대로.
- 단어 슬롯머신: 24단어, 4단계 가중치, 단어별 색·액세서리(SVG 22종)·눈 표정, 착지 시 팝·색종이, sessionStorage 유지. 팔레트는 `_local/drafts/word-palette.json`(`DESIGN_SYSTEM.md` §1.4).
- 캡처 `_local/drafts/shots2d/`, 시트 `sheet2d-play.jpg`, `sheet2d-words.jpg`.

### 단계 4 — 전환과 제품 배치 (§12-4)

목표: 같은 토끼와 팀 이름이 첫 화면에서 제품 장면으로 이어지는 방식을 정한다.

작업:
1. 캔버스 1개를 고정 배치하고 스크롤 위치에 따라 토끼 그룹을 첫 화면 자리 → Typer 장면의 자리로 이동. 스크롤이 멈추면 이동도 멈춤. 확대·마스크 전환 없음.
2. Typer 장면에서 토끼가 키보드(`keyboard.js`)의 `tp:key` 이벤트에 반응. 도파민대학교 장면에서는 테스트 커버 호버에 시선만 반응.
3. 모바일: 겹침 없이 세로 흐름. 토끼는 첫 화면에만 3D로 두고 제품 장면에서는 작은 고정 위치 또는 생략 중 택1.
4. 위로 스크롤 시 원래 구도 복귀. 전환 중 조작 시 순간 이동 없음.

사용자 결정: 제품 장면에서의 토끼 위치, 모바일 처리, 제품 페이지 이동 시 유지 방식.

완료 조건: 데스크톱·모바일·짧은 화면·가로 모드에서 팀 이름이 항상 읽히고 다음 작품을 가리지 않음.

### 단계 5 — 실제 적용 (사용자가 시작을 요청한 뒤, §12-5)

진행 (2026-09-13, 사용자 "draft 말고 실제 개발"):
- `base.css`를 디자인 시스템 토큰으로 교체(레거시 별칭으로 `typer.css`·`dopamine.css` 호환), `home.css` 신설, `hero.js`(토끼 + 단어 슬롯머신 + 단어별 반응 소품 25종) 신설, `logo.js` 삭제, `index.html` 재작성, `i18n.js` 키 추가·줄표 제거, `404.html` 추가, `README.md` 갱신.
- 서체: Bricolage Grotesque latin·latin-ext 자체 호스팅(`assets/fonts/`, OFL 동봉). Wanted Sans는 CDN woff2를 메트릭 오버라이드 @font-face로 감쌈.
- 단어 25개(제품 가치 기준): common joyful·calm·cozy·playful·clicky·curious·cheerful·chatty / uncommon witty·surprising·retro·rhythmic·aesthetic·thoughtful·satisfying·dramatic·connected / rare exciting·ASMR·insightful·dopamine·sparkly / epic smashing·enlightened·mint choco. 도파민대학교의 내면 성찰·관계는 thoughtful·connected·insightful·enlightened로 에둘러 표현.
- 반응 소품: 키캡(진짜 타건음), 공→골대, mp3(타건음 리듬 재생), 커피 김, 모니터 깜빡임, 선인장 흔들림, 말풍선 순환, 느낌표·번개(토끼 점프), zzz(잠깐 깸), 해(광선 회전), 외알 안경 떨어짐, 돋보기(눈 확대), 친구·거울(눈 감기), 폭죽(색종이), 별 반짝임, 졸업장 펼침, 후광, 아이스크림 베어 먹기, satisfying·smashing 키캡 부서짐.
- 검증: 홈·Typer·도파민·404 캡처에서 콘솔 오류 0. 정지 시 렌더 0.
- 다듬기(2026-09-13): 소품은 토끼 몸을 가리지 않는 세 자리만 쓴다(얼굴 왼쪽 빈자리, 등 위, 귀 사이). 단어별 귀 자세(calm·ASMR·thoughtful·enlightened 처짐, surprising·exciting·dopamine·smashing 쫑긋). 제품 링크는 알약 대신 제품 아이콘 + 단어색 텍스트 링크. 상단바 `다시 뽑기` 제거(손잡이가 유일한 조작). 커서 48px로 확대, 그림자.

목표: 합의된 한 방향만 사이트에 반영하고 §11로 검증한 뒤 PR.

작업:
1. `index.html` 첫 화면 마크업 교체(카피·모서리 표시·오버레이 버튼·폴백 자리), 인라인 스타일을 `home.css`로 분리.
2. `hero3d.js` — 장면 구성, 재질 프리셋, 다시 뽑기, 상호작용, 스크롤 연결, 온디맨드 렌더, 정리(dispose), 폴백 전환.
3. `i18n.js` — 키 추가: `home.copy`(영문 유지), `home.sub`, `home.corner`, `home.reroll`, `home.next`. 기존 `home.rabbit.action` 재사용.
4. 재질·색·배경·액세서리 조합표를 `hero3d.js` 데이터로 고정. 내비게이션 색은 조합에 포함하지 않음.
5. `sessionStorage`로 같은 방문 안 상태 유지. 수동 다시 뽑기는 스크롤 위치와 Typer 커스텀을 건드리지 않음.
6. 제품 경험(§10): Typer는 부품 선택 방식 비교 후 하나 적용, 모니터 안 정보 정리. 도파민대학교는 홈 체계 확정 후 카피·테스트 배열 조정. 별도 PR로 분리 가능.
7. `tokens.css`를 `base.css`에 이식하고 폰트를 `assets/fonts/`에 자체 호스팅(Bricolage Grotesque, Wanted Sans). 아이콘은 Phosphor 한 가족. `i18n.js` 메타 제목의 줄표 제거.
8. `404.html` 추가(토끼 + 한 줄 + 홈 링크). 벤토 제품 영역·타이포 푸터는 홈 체계 확정 후 같은 PR 또는 후속 PR.
9. `README.md` 갱신 — 현재 파일 구조와 맞지 않는 항목(`styles.css`, `main.js`, `NewMeans Web Assets/`) 정리, `_local/` 규칙 명시.
10. 검증(§4 참고) 후 PR. PR 본문에는 결정과 측정 결과만 쓴다.

완료 조건: §11 "완료 전에 확인할 것" 9개 항목 전부 체크, 측정값 첨부.

## 4. 검증 방법 (§11 대응)

| 확인 항목 | 방법 |
|---|---|
| 첫 콘텐츠 표시 | 3D 스크립트를 `defer` + 카피 렌더 후 로드. Lighthouse FCP/LCP, 스크립트 차단 시에도 카피·링크 표시 확인 |
| 전송량 | DevTools Network(압축 기준) 첫 3D 표시까지 합계 ≤ 600KB |
| 삼각형 수 | `renderer.info.render.triangles` ≤ 20,000 |
| 프레임 | `?perf=1`일 때 프레임 시간 오버레이. 데스크톱 60fps, 모바일 30fps |
| 정지 상태 | 조작 후 1초 뒤 `requestAnimationFrame` 호출 0 확인 |
| 누수 | 다시 뽑기 50회 + 스크롤 왕복 후 `renderer.info.memory`(geometries, textures)와 리스너 수 불변 |
| 반응형 | 1440×900, 1280×720, 390×844, 844×390(가로), 글자 크기 200% 스크린샷 |
| 접근성 | Tab 순서, 스크린리더 텍스트(`NewMeans`·카피), 포커스 표시, `prefers-reduced-motion` 동작 |
| 대비 | 팔레트 조합별 카피·보조 텍스트 대비 AA 이상 |
| 폴백 | WebGL 비활성 브라우저 플래그로 2D 구성 확인 |

스크린샷과 접근성 스냅샷은 Playwright로 자동화한다. 측정 원본은 `_local/notes/`, 요약은 PR 본문.

## 5. 미리 짚어둘 충돌과 위험

- 카피 3안 `Come play a little.`은 명령형이라 카피 규칙(명사형·재치 시도 금지)과 `DESIGN_PLAN.md` §5의 "명령처럼 느껴지지 않게"에 모두 걸린다. 1안 → 2안 순서로 비교하고 3안은 제외를 권한다.
- 좌하단 `Seoul, Korea`는 짧은 사실 표기라 허용한다. 팩트 시트의 지역 규칙은 "서울에 있는 작은 2인 스튜디오" 같은 설명형 카피를 막는 것이다.
- 픽셀 조각의 외곽선은 계단이 많아 `/`·`(`·`)` 같은 얇은 조각의 정점 수가 크다. 25개 형상 × 베벨이면 20,000 삼각형을 넘길 수 있어, 둥글림 옵션과 베벨 단 수를 단계 2에서 측정으로 정한다.
- Clear Mint(transmission)는 장면을 한 번 더 렌더한다. 모바일 기본 재질에서는 뺄 가능성이 높다.
- 390px 폭에서 글자 하나의 폭이 약 40px라 개별 터치가 어렵다. 묶음 규칙을 처음부터 넣는다.
- 폰트 후보는 모두 OFL이지만 선택 후 한글 범위와 라이선스를 다시 확인한다. 비교 보드 외에는 선택된 폰트만 로드한다.
- 민트 원색 워드마크는 `#FCFBF7` 배경에서 대비 1.9:1이다. 로고형이라 텍스트 대비 기준의 대상은 아니지만 판독성은 크기로 확보하고, 내비게이션·푸터의 작은 워드마크는 잉크 버전을 유지한다.
- `main` 머지 = 즉시 배포. 단계 1–4의 산출물은 `_local/drafts/`나 작업 브랜치에만 둔다.

## 6. 다음 액션

1. 단계 1 비교 보드 제작 → 스크린샷과 대비표 전달.
2. 사용자 결정 → `DESIGN_PLAN.md` §2 갱신 → 단계 2.
