# NewMeans 웹사이트 디자인 계획

상태: 사용자 협의용 계획. 구현 보류.

업데이트: 2026-09-12

## 1. 이번 문서의 역할

뉴민스가 즐거운 디지털 경험을 만든다는 것을, 설명보다 직접 반응하는 토끼와 작품으로 전달한다. 첫 화면은 부드럽고 귀엽고 여유로운 인상으로 구성한다.

이 문서는 최신 사용자 요청을 기준으로 한다. 이전 비교 시안의 매우 굵은 검정 제목, 키캡이 화면 전체로 확대되는 전환, 토끼와 팀 이름을 가로로 분리한 배치는 채택하지 않는다. 기존 사이트 및 실험 샘플은 최종 디자인의 기준이 아니다.

이번 작업은 계획 작성까지다. 사이트 HTML/CSS/JS, 3D 모델, 패키지, 배포 설정을 변경하지 않는다. 후속 구현은 사용자가 시작을 요청한 뒤 진행한다.

### 기록과 공개 범위

- 외부 디자인 레퍼런스의 사이트 이름과 URL을 저장소, 코드 주석, 에셋명, 커밋 메시지, PR 제목·본문에 기록하지 않는다.
- 외부 사이트의 화면 캡처, HTML, 소스, 조사 메모를 Git에 추가하지 않는다.
- 저장소에는 뉴민스의 요구사항, 디자인 결정, 자체 제작물, 검증 결과만 남긴다.

## 2. 확정된 방향과 미확정 항목

| 항목 | 확정된 방향 |
|---|---|
| 첫 화면 구도 | 좌측 상단 카피 / 우측 하단 토끼와 팀 이름 |
| 브랜드 구성 | 토끼 아래에 `NewMeans`. 둘을 하나의 시각적 구성으로 취급 |
| 3D 범위 | 토끼의 각 요소와 `NewMeans`의 여덟 글자를 개별 3D 요소로 구성 |
| 초기 상태 | 토끼와 팀 이름은 완전히 평면이고 브랜드 원색(`#85C6A8`). 3D 리빌은 보류(2026-09-13, 애니메이션과 함께 쓰면 렉이 걸려 2D로 진행) |
| 인상 | 부드럽고 귀엽게. 검정 초고중량 타이포의 강압적인 인상 완화 |
| 언어 | 한국어 버전에서도 주요 카피는 영어 사용 가능. 설명과 조작 안내는 한국어 |
| 상호작용 | 마우스·카피·토끼·팀 이름·주변 오브젝트가 서로 반응 |
| 움직임 | 3D 깊이와 작은 회전. 항상 크게 흔들거나 뛰지 않음 |
| 전환 | 토끼의 정체성과 상태가 다음 장면에 이어짐. 버튼 확대 없음 |
| 변주 | 헤드라인 강조어 옆의 슬롯머신 손잡이. 당기면 단어가 릴처럼 돌다가 가중치 확률로 멈추고, 단어색·토끼색·액세서리·눈 표정이 그 단어의 컨셉으로 바뀐다. 토끼 형상은 유지 (2026-09-13) |
| 안내 | 설명 문구를 최소화하고 표정·커서·눌림·방향 표시로 조작을 전달 |
| 성능 | 콘텐츠 접근을 먼저 보장하고 3D 비용을 제한 |

확정(2026-09-12): 색·서체·형태 토큰은 `DESIGN_SYSTEM.md` v1.1. 좌측 하단 표시는 `Seoul, Korea`. 아이브로 없음. 작업용 영문 카피는 `Life, a little more joyful.`(후보 유지, 최종 확정 전).
아직 확정하지 않은 항목: 최종 영문 카피, 첫 방문 기본 재질, 변주 팔레트, 액세서리 모양. 아래 수치와 후보는 검토 시작점이며 승인된 최종 디자인이나 측정 결과가 아니다.

## 3. 첫 화면의 공간 구성

### 데스크톱

```text
┌──────────────────────────────────────────────────────────┐
│ 작은 내비게이션                       언어 · 다시 뽑기   │
│                                                          │
│ 짧은 영문 카피                                           │
│ 필요할 때만 한 줄 설명                                   │
│                                                          │
│                                     토끼                 │
│                                                          │
│                              N e w M e a n s              │
│                                                          │
│ 위치 또는 스튜디오 정보                           ↓       │
└──────────────────────────────────────────────────────────┘
```

- 카피와 3D 브랜드 구성 사이의 대각선 여백이 첫 화면의 중심이다. 이 공간을 카드·설명·큰 버튼으로 채우지 않는다.
- 카피 영역은 화면 너비의 약 6–45%, 높이의 약 17–38%를 시작점으로 검토한다.
- 토끼와 팀 이름은 너비의 약 48–93%, 높이의 약 42–84% 안에서 구성한다. 팀 이름 전체가 첫 화면에서 읽혀야 한다.
- 토끼와 팀 이름의 관계는 스크롤·재질 변경 후에도 유지한다. 한쪽만 다른 곳에 고립시키지 않는다.
- 3D 카메라는 정면에 가까운 시점을 기본으로 하고, 약한 깊이 차이와 접지 그림자로 입체감을 만든다.
- 토끼가 팀 이름 위에 놓인 인상을 주되, 실제 글자를 가리거나 눌러 읽기 어렵게 만들지 않는다.
- 화면 테두리 안전 여백은 데스크톱 32–56px, 모바일 20–24px에서 검토한다.

### 화면 모서리

| 위치 | 역할 | 표현 후보 |
|---|---|---|
| 좌측 하단 | 작고 사실적인 정체성 정보 | `Seoul, Korea` 또는 `Independent studio` 중 하나 |
| 우측 하단 | 다음 장면으로 가는 방향 | 얇은 아래 화살표. 보이는 설명은 생략 |
| 우측 상단 | 최소한의 유틸리티 | 언어 전환, `다시 뽑기` 틴트 알약(아이콘 + 글자, 2026-09-13), `메일 보내기` |

상단 내비게이션(확정 2026-09-13): 좌측 잉크 워드마크, 중앙 링크 알약 컨테이너(`제품`·`스튜디오`), 우측 언어 세그먼트 + 다시 뽑기 + `메일 보내기` 알약. 첫 화면 배경은 페이퍼 단색. 토끼 뒤 글로우·점 격자는 쓰지 않는다.

기존 소개 문서에는 서울의 2인 스튜디오로 기재되어 있다. 위치를 표시한다면 게시 전에 현재 정보인지 확인한다. 근거 없는 실시간 위치·시계·상태 표시를 만들지 않는다.

모서리 표시는 작은 크기로 일관되게 배치한다. 아래 화살표와 3D 팀 이름 사이에는 독립된 클릭 공간을 확보한다. 첫 화면에 대형 CTA, 다운로드 버튼 묶음, 상세 커스텀 패널을 동시에 넣지 않는다.

### 모바일과 짧은 화면

- 좌측 상단 카피 → 그 아래 우측으로 치우친 토끼와 팀 이름 → 하단 모서리 정보 순서로 재배치한다.
- 데스크톱 화면을 통째로 축소하지 않는다. 팀 이름의 판독성과 터치 영역을 따로 확보한다.
- 높이가 부족하면 자연스럽게 페이지가 길어지도록 한다. 토끼·글자·화살표를 겹쳐서 한 화면에 강제로 넣지 않는다.
- 가로 모드, 큰 글자 설정, 브라우저 주소 표시줄 변화도 레이아웃 검토 대상에 포함한다.

## 4. 색상과 타이포그래피

색·서체·형태의 확정 토큰과 규칙은 `DESIGN_SYSTEM.md`가 기준이다. 아래 표는 그 시스템으로 옮기기 전의 검토 기록이다.

### 색상 역할

| 역할 | 기준 또는 검토 후보 | 사용 원칙 |
|---|---|---|
| 뉴민스·Typer 원색 | `#85C6A8` | 로고 및 토끼의 기본 정체성 |
| 도파민대학교 원색 | `#FF6666` | 해당 작품과 제한된 액세서리 강조 |
| 기본 배경 후보 | `#FCFBF7` | 밝고 따뜻한 배경 |
| 주요 카피 후보 | `#514D48` | 순검정보다 부드러운 웜 차콜 |
| 보조 텍스트 후보 | `#69645B` | 충분한 대비를 갖는 짧은 설명 |

- 귀여운 인상은 색을 많이 추가하기보다 둥근 형태, 작은 움직임, 낮은 시각적 밀도로 만든다.
- 밝은 민트를 작은 본문 글자에 그대로 쓰지 않는다. 모든 조합에서 실제 배경과의 텍스트 대비를 확인한다.
- 기본 팔레트와 랜덤 변주 팔레트를 구분한다. 토끼를 다시 뽑아도 내비게이션과 조작 체계가 임의의 색으로 바뀌지 않는다.
- 금속·투명 재질에서도 원색의 인지가 사라지지 않는 조명과 배경을 선택한다.

### 폰트 후보

1차 후보(Nunito Sans / Manrope)는 사용자 검토에서 "너무 평이하다"로 제외했다. 2차는 표정 있는 서체와 강조어 처리를 한 묶음으로 본다.

3차(디자인 시스템 v1): 사용자가 지정한 안티슬롭 프론트엔드 스킬의 규칙에 따라 세리프 디스플레이(A·B)는 기본 선택에서 제외했다. 영문 디스플레이는 Bricolage Grotesque 600, 강조는 같은 서체 안에서 색으로, 한국어와 UI는 Wanted Sans. 픽셀 서체는 워드마크와 마이크로 라벨만. 세부는 `DESIGN_SYSTEM.md` §2.

| 방향 | 영문 주요 카피 | 강조어 처리 | 한글 | 함께 가는 요소 |
|---|---|---|---|---|
| A | Instrument Serif 400 | 이탤릭 + 민트 텍스트색 | Noto Serif KR 500 (제목) / Wanted Sans (설명) | 토끼 뒤 민트 글로우, 알약 링크 컨테이너 |
| B | Fraunces 500 (SOFT·WONK) | 브랜드 픽셀 서체(DNF BitBit) 민트 | Noto Serif KR 500 / Wanted Sans | 점 격자 배경, 픽셀 아이브로·모서리 문구, 작은 CTA 알약 |
| C | Bricolage Grotesque 600 | 픽셀 점선 밑줄 | Wanted Sans 700 / 400 | 떠 있는 알약 내비게이션 |
| D | Gabarito 700 | 연민트 형광 강조 | Wanted Sans 700 / 400 | 투톤(코랄·민트) 글로우, 민트 CTA 알약 |

- 최종 폰트는 같은 카피와 같은 배치로 비교하고 선택한다.
- 영문 제목은 문장형 대소문자와 1–2행을 우선 검토한다. 모든 제목을 대문자·900 굵기로 통일하지 않는다.
- 제목은 데스크톱 52–76px, 모바일 34–46px를 시작점으로 검토한다. 크기만으로 화면을 압도하지 않는다.
- 한글 설명은 14–16px, 짧은 내비게이션과 모서리 정보는 12–14px를 시작점으로 한다.
- 화면에 실제 표시할 폰트만 로드한다. 최종 선택 후 폰트 라이선스와 한글 지원 범위를 확인한다.
- `NewMeans`의 3D 글자는 본문 폰트로 새로 타이핑하지 않고, 기존 브랜드 워드마크의 글자 윤곽을 기준으로 제작한다.

## 5. 카피 원칙과 후보

카피는 토끼와 함께 놀 수 있는 분위기를 열어주는 한 문장만 담당한다. 즐거운 디지털 경험이라는 주장은 인터랙션과 실제 작품이 뒷받침한다.

### 홈 주요 카피 후보 — 미확정

사용자 방향: "조금 더 즐거운 일상" — 우리가 일상을 조금 더 즐겁게 만든다는 뉘앙스. 강조어(이탤릭·픽셀·형광 처리 대상)는 `joyful`/`joy`/`즐거운`.

| 후보 | 형태 | 비고 |
|---|---|---|
| `Life, a little more joyful.` | 명사구 | 가장 짧고 강조어가 문장 끝에 옴 |
| `A little more joy, every day.` | 명사구 | "매일"이 들어가 일상 뉘앙스가 분명 |
| `Making everyday life a little more joyful.` | 동명사구, 2행 | 만드는 주체가 드러남 |
| `We make your days a little more joyful.` | 문장 | 사용자가 준 문장에 가장 가까움. 영문에 한해 문장형 허용 여부를 함께 결정 |
| `조금 더 즐거운 일상.` | 한글 헤드라인 | 한국어 화면에서 영문 대신 쓰는 안. 이때 설명은 영문 `Games and AI services by NewMeans` |

한글 설명 후보: `일상을 조금 더 즐겁게 만드는 게임과 AI 서비스` / `조금 더 즐거운 일상을 위한 게임과 AI`.

- 한국어 화면에서도 위 주요 카피는 영어로 유지할 수 있다.
- 한글 설명은 한 줄로 제한한다. 작품 영역에서 정체성이 충분히 전달되면 홈 설명은 생략하는 안도 비교한다.
- 제품 이름, 테스트 이름, 실제 기능은 정확하게 표시한다.
- 보고서형 문장, 과장된 제작 선언, 의미가 겹치는 슬로건, 반복되는 감탄사를 사용하지 않는다.
- 주요 카피의 클릭 반응은 문장을 새 문장으로 교체하거나 레이아웃을 무너뜨리지 않는다.
- 본문·조작 문구는 한국어 화면에서 한국어로 표시한다. 스토어 배지는 공식 언어별 이미지를 유지한다.

## 6. 토끼와 팀 이름의 3D 구성

### 토끼

- 지금의 윤곽과 요소 배치를 유지한다. 일반적인 동물 캐릭터로 대체하지 않는다.
- 각 요소는 직각으로 압출한다(베벨 없음). 정면에서는 2D 로고와 똑같이 평면으로 보이고, 기울어질 때만 두께가 드러난다. (2026-09-13 확정)
- 두께는 조각마다 그 조각의 획 폭과 같게 해 단면이 정사각형이 되게 한다. 측정값: 글자 획 120, 토끼 괄호·귀 48–50, 눈 54, 코 44, 앞발 42, 아치 32, 고리 24 (로고 좌표 단위). (2026-09-13 확정)
- 앞면·뒷면은 조명 없이 브랜드 원색 `#85C6A8` 그대로 칠하고, 옆면만 조명을 받는다(압출 지오메트리의 두 재질 그룹). 정면 상태의 색이 평면 로고와 픽셀 단위로 같아지고, 기울일 때 옆면 음영으로만 입체가 드러난다. (2026-09-13)
- 귀는 뿌리, 눈·코는 각 요소의 중심을 기준으로 작은 3D 움직임을 갖는다.
- 앞뒤 깊이 이동과 X/Y축 회전으로 표정을 만든다. 재질의 반사와 굴절이 작은 움직임에서도 보이게 조명을 설계한다.
- 액세서리는 토끼 윤곽과 표정을 가리지 않는 크기로 제한한다.
- 처음에는 정면 시점·무조명으로 렌더해 기존 평면 로고와 같은 실루엣과 원색으로 보인다. 두께·모서리·재질·그림자는 상호작용이 시작된 뒤에만 드러난다.

### `NewMeans`

- `N`, `e`, `w`, `M`, `e`, `a`, `n`, `s`를 개별적으로 반응 가능한 3D 요소로 구성한다.
- 기본 순서, 자간, 기준선은 고정한다. 사용자가 조작하지 않을 때는 하나의 워드마크로 읽혀야 한다.
- 동일한 글자 윤곽과 재질은 재사용하되 각 글자의 반응은 독립적으로 제어한다.
- 토끼와 같은 조명·카메라 공간에 둔다. 서로 다른 이미지가 화면에 얹힌 것처럼 보이지 않게 한다.
- 토끼를 만지면 가까운 글자에 작은 반응이 전달되고, 글자를 누르면 토끼가 그 글자를 바라보도록 한다.
- 글자가 바닥으로 떨어지거나 문장 밖으로 날아가는 물리 효과는 기본 동작에 포함하지 않는다.

### 모션 강도의 시작 기준

| 대상 | 검토 범위 | 의도 |
|---|---|---|
| 토끼 전체 | 좌우 약 ±5°, 상하 약 ±3° 회전 + 기존 사이트(main)와 같은 위치 이동: 얼굴 1.3, 귀 0.6, 몸 0.45, 글자 0.12 × (로고 폭의 3%) | 포인터를 따라 얼굴이 실제로 움직임 |
| 귀·눈·코 | 추가 회전 약 2–4°, 요소 두께 이내의 깊이 변화 | 작지만 입체적인 표정 |
| 팀 이름의 글자 | 약 2–3° 회전, 글자 높이의 2–4% 이내 눌림 | 누르는 감각과 판독성 유지 |
| 직접 조작 반응 | 즉시 시작, 약 180–350ms 복귀 | 응답이 빠르고 과하게 튀지 않음 |

수치는 최종 모델과 기기에서 조정할 목표다. 상시 반복 운동은 기본 상태에 넣지 않는다.

## 7. 설명 없이 상호작용을 알아보게 하는 규칙

| 사용자 행동 | 카피 또는 오브젝트 반응 | 토끼와의 연결 |
|---|---|---|
| 포인터가 장면에 들어옴 | 조작 가능한 영역 가까이에서만 약한 반응 | 눈과 고개가 포인터를 바라봄 |
| 카피의 지정된 단어에 가까이 감 | 해당 단어의 색·깊이감이 조금 변하고 포인터 커서 표시 | 토끼가 그 단어로 시선을 옮김 |
| 카피의 단어를 누름 | 한 단어만 짧게 눌렸다가 복귀. 줄바꿈과 위치 유지 | 토끼가 그 단어를 본 뒤 글자 하나가 튀어오름(poke) |
| 토끼를 누름 | 글자 하나가 튀어올랐다 제자리로 돌아옴 (기존 사이트의 poke) | 글자가 돌아올 때까지 토끼가 안절부절: 눈이 동그래지고 발을 구르고 꼬리를 흔들고 땀이 튐 |
| 팀 이름의 글자를 잡아끎 | 글자가 포인터를 따라오고, 놓으면 스프링처럼 제자리로 | 잡혀 있는 동안 토끼가 안절부절 (위와 같음) |
| 글자를 짧게 탭 | 그 글자가 작게 튀어오름 | 위와 같음 |
| 키보드·작품 오브젝트를 누름 | 실제 타건음이나 해당 제품의 즉각적인 반응 | 토끼가 그 오브젝트를 바라보며 반응 |
| 아래 화살표에 접근·클릭 | 작은 위치 변화·눌림 | 다음 장면으로 이동하면서 같은 토끼가 유지됨 |

### 발견 가능성

- 클릭 대상으로 지정한 카피 단어와 3D 글자는 호버 이전에도 눌릴 수 있는 표면·미세한 입체감이 느껴져야 한다.
- 호버, 포커스, 누름의 반응을 같은 체계로 만든다. 방문자가 한 번 배운 규칙을 다른 오브젝트에서도 사용할 수 있게 한다.
- 커서 접근에 반응이 생긴 뒤 클릭으로 더 분명한 반응이 이어져야 한다. 호버만으로 장식 효과가 완결되지 않게 한다.
- 첫 방문에 한 번만, 토끼가 카피를 보고 가까운 글자를 건드리는 짧은 행동을 보여주는 안을 검토한다. 사용자가 먼저 조작하면 이 행동을 취소한다. 반복 안내나 강제 대기는 없다.
- `Click me`, `토끼를 눌러보세요` 같은 상시 설명, 튜토리얼 팝업, 떠다니는 도움말은 기본안에 넣지 않는다.
- 3D가 드러나는 순간은 첫 스크롤이다(2026-09-13 확정). 페이지 맨 위에서 스크롤하면 약 1,100px 분량 동안 페이지는 움직이지 않는다. 그동안 카피는 한 줄(약 26px)로 작아지고, 토끼와 팀 이름은 커지면서(최대 1.5배, 화면 폭의 82% 이내) 화면 가운데로 이동한다. 앞 55%(약 600px)는 카메라가 천천히 내려가 조각들의 밑면이 보이고, 그다음 약 400px은 밑면이 보이는 채로 아주 느리게만 움직이는 멈춤 구간이다(스크롤 입력은 관성으로 따라온다). 끝을 지나면 카메라가 살짝 위로 돌아와 3D 상태가 유지되고, 이후 스크롤은 평소처럼 페이지를 내린다. 완료 전에 위로 스크롤하면 되돌아간다. 밑면이 보이는 상태는 유지하지 않는다. 글자·토끼를 먼저 누르면 같은 경로를 700ms 애니메이션으로 지나간다. 키보드(↓·PageDown·Space)도 같다. 움직임 줄이기·중간 진입 시에는 즉시 3D 상태. 페이지 로드 직후 자동으로 기울이거나 조명을 켜지 않는다. 드러나기 전의 반응(시선, 귀 움직임)은 평면 상태를 유지한 채 2D로 표현한다. 트리거는 행동 규칙 단계에서 정한다.
- 기능을 가진 화살표·다시 뽑기에는 텍스트 대체 이름을 제공한다. 화면에는 최소한의 기호만 보이게 한다.

### 모바일·키보드

- 터치에서는 첫 탭에 즉시 같은 반응을 준다. 호버를 먼저 거쳐야 작동하는 구조는 사용하지 않는다.
- 작은 3D 요소의 실제 클릭 영역은 외형보다 넓게 확보한다. 약 44px 터치 영역이 서로 겹치면 개별 글자 조작을 그룹 단위로 단순화한다.
- Tab과 Enter/Space로 접근할 수 있는 대응 동작을 마련한다. 드래그·마우스 위치만으로 필요한 기능을 수행하게 하지 않는다.
- 움직임 줄이기 설정에서는 표정의 상태 변화·작품 이동·일반 탐색을 유지하면서 이동량과 회전을 줄인다.
- 카피의 실제 문자열과 팀 이름은 접근 가능한 텍스트로도 제공한다. 문장의 읽기 순서는 시각 효과와 독립적으로 유지한다.

## 8. 장면을 연결하는 전환

연결감은 물체를 크게 늘리는 효과가 아니라, 같은 토끼·팀 이름·선택 상태가 다음 화면에도 존재하는 데서 만든다.

1. 첫 화면에서 토끼와 팀 이름이 하나의 구성을 이룬다.
2. 스크롤이 시작되면 다음 장면의 배경과 작품이 화면 가장자리에서 들어온다.
3. 토끼와 팀 이름은 읽을 수 있는 크기를 유지하며, 다음 작품을 가리지 않는 위치로 부드럽게 이동한다.
4. 작품 장면이 자리 잡으면 토끼가 그 작품의 오브젝트와 반응한다. 글자와 토끼를 계속 누를 수 있다.
5. 위로 스크롤하면 동일한 상태로 첫 구도로 돌아간다.

- 키캡·버튼을 화면 크기로 확대하지 않는다. 스토어 배지나 카피의 글자도 전환용 마스크로 쓰지 않는다.
- 레이아웃 이동과 사용자 조작의 영향을 분리한다. 전환 중 누른다고 토끼가 기준 위치로 순간 이동하면 안 된다.
- 카피를 3D 장면 속 이미지로 굳히지 않는다. 텍스트의 선명도와 접근성을 유지하면서 반응만 연결한다.
- 스크롤을 멈추면 장면 이동도 멈춘다. 강제 스크롤, 장시간 고정, 마지막 프레임까지 기다려야 하는 탐색은 사용하지 않는다. (3D 리빌 스크롤 연출은 3D와 함께 보류)
- 토끼와 워드마크의 재질·액세서리·조합은 장면 전환으로 다시 뽑히지 않는다.
- 모바일에서는 장면 겹침을 줄이고 위아래 흐름을 우선한다.

## 9. 재질과 다시 뽑기

### 단어 슬롯머신 (2026-09-13 확정 방향)

- 위치: 헤드라인 `Life, a little more ___.`의 마침표 옆, 헤드라인 크기에 비례하는 손잡이(공 + 막대 + 받침). 상단바의 `다시 뽑기`도 같은 동작.
- 릴: 현재 단어 → 무작위 단어 18개 → 결과 순으로 세로 릴이 1.7초 동안 ease-out으로 돌고(초반 흐림), 단어 폭은 결과 폭으로 전환, 착지 시 작은 반동. 도는 동안 토끼는 흥분 상태(동그란 눈, 발 구르기, 꼬리, 땀), 착지하면 색·액세서리가 팝으로 바뀌고 글자 하나가 튀어오른다. 희귀·에픽은 색종이가 터진다.
- 단어는 Typer와 도파민대학교가 주는 것들 24개. 각 단어에 색상 계열(단어색은 700단계로 대비 4.5:1 이상, 토끼는 400단계), 액세서리, 눈 표정, 희귀도를 정해 둔다. 완전 무작위가 아니라 가중치: common 10 / uncommon 6 / rare 3 / epic 1 (epic 약 2%, rare 약 10%). 꾸밈 요소가 많은 단어일수록 희귀.
- 목록(2026-09-13 개정, 제품 가치 기준): common joyful(민트) · calm(세이지, zzz, 감은 눈) · cozy(피치, 커피) · playful(코랄, 공+농구 골대) · clicky(틸, 키캡) · curious(라벤더, 돋보기) · cheerful(골드, 해) · chatty(페리윙클, 말풍선). uncommon witty(플럼, 외알 안경) · surprising(앰버, 느낌표) · retro(올리브, 선글라스+레트로 모니터) · rhythmic(스카이, 헤드폰+음표+mp3) · aesthetic(코코아, 선인장+모니터) · thoughtful(인디고, 거울+생각 풍선) · satisfying(라임, 키캡+별) · dramatic(차콜, 스포트라이트+눈물) · connected(로즈, 친구 토끼+하트). rare exciting(코랄, 고깔+폭죽) · ASMR(틸, 헤드폰+음파) · insightful(라벤더, 학사모+안경+졸업장) · dopamine(마젠타, 번개) · sparkly(골드, 별). epic smashing(코랄, 공+키캡 부서짐) · enlightened(골드, 후광+별) · mint choco(민트, 아이스크림+키캡). 제외: tactile, golden, holographic, legendary, focused, mindful, dreamy, genius.
- 같은 방문 안에서는 마지막 단어를 유지한다(sessionStorage). 손잡이는 버튼이라 키보드로도 당길 수 있고, 결과는 `role=status`로 읽힌다.
- 워드마크도 토끼와 같은 색으로 물든다(2026-09-13). 내비게이션·조작 색은 바뀌지 않는다. 제품 단어에는 그 제품으로 가는 버튼이 카피 아래에 나타난다. 소품 중 상당수는 눌러서 반응한다(키캡 타건음, 공 던지기, mp3 리듬 등). 서 있는 소품은 워드마크 기준선을 바닥으로 삼아 워드마크 왼쪽 빈 공간에 놓이며 토끼 몸에는 붙이지 않는다. 카피 뒤 공간은 카피를 가리지 않는 한 소품이 모두 쓸 수 있다. 글자는 첫 화면 어디로든 끌고 갈 수 있다. 공은 끌어서 던지는 미니게임(골대 득점, 키캡 넘어뜨리기), 커피·아이스크림은 토끼 입에 가져다 줄 수 있다.
- 슬롯 창은 항상 보이고(안쪽 그림자, 단어색 틴트), 가장 긴 단어 기준 고정 폭에 좌측 정렬, 마침표도 창 안에 있다(단어마다 모양이 다를 수 있음). 손잡이는 앞으로 당겨지는 동작. 보조 카피는 영어 고정. 커서는 첫 화면에서 브랜드 반짝이로 바뀌고, 글자 위에서는 손, 잡으면 쥔 손, 소품 위에서는 점이 된다. 글자를 가리키면 글자가 살짝 들려 올라와 뺏을 수 있음을 보여준다. 땀은 기존 사이트와 같은 위치(로고 폭 56%, 높이 31%).


### 재질 시안 후보

| 후보 | 목표 표현 | 용도 |
|---|---|---|
| Soft Mint | 둥근 모서리와 부드러운 무광·반무광 | 기본안 우선 검토 |
| Clear Mint | 약한 색을 가진 젤리·반투명 수지 | 작은 3D 움직임과 굴절 표현 |
| Milky White | 유백색 표면에 민트·코랄 액세서리 | 원색 배경과 조합하는 대안 |

- 표면의 반사만 바꾼 것과 실제 투명 재질의 차이를 구분해 시안을 검토한다.
- 검토 기록(2026-09-13): 원본 픽셀 외곽선을 그대로 압출한 변형은 삼각형 14만으로 예산을 넘겨 제외. 곡선 조각만 1.2px 허용오차로 단순화하고 글자 계단은 유지하는 변형(13,854 삼각형)으로 진행한다.
- 완전 투명으로 눈·코·글자 형태가 사라지지 않게 한다. 워드마크 판독성을 특히 우선한다.
- 액세서리는 작은 리본·헤드폰 등 제한된 종류를 후보로 한다. 무거운 털 시뮬레이션이나 큰 소품은 첫 범위에 넣지 않는다.
- 재질·배경·액세서리를 무제한 무작위 RGB로 섞지 않는다. 어울리는 조합을 미리 정하고 그 안에서 선택한다.
- 첫 진입/브라우저 새로고침과 수동 다시 뽑기는 구분한다. 수동 다시 뽑기는 스크롤 위치와 키보드 커스텀을 유지한다.
- 제품 페이지로 이동해도 같은 방문 안에서는 같은 토끼를 유지하는 방향을 검토한다.
- 재질을 바꿀 때마다 모델을 재다운로드하거나 새 렌더러를 만들지 않는다.
- 수집 목록·등급·희귀도·계정·보상 시스템은 현재 범위에 포함하지 않는다.

## 10. 홈 이후의 제품 경험

### Typer

- 드롭다운 없이 이미지로 고르는 커스터마이징을 유지한다.
- 키보드와 모니터를 중심으로 정렬하고, 케이블은 정렬 계산에서 제외한다. 키보드에 상대 배치한 뒤 옆으로 뻗는 부분을 장면 마스크로 자른다.
- 큰 키보드 가까이에서 부품을 고르는 방식과 간결한 선택 탭을 비교한다. 첫 화면부터 모든 부품을 펼치지 않는다.
- 실제 타건음과 실제 게임 에셋을 사용한다. 웹에서 게임 플레이를 임의로 흉내 낸 프리뷰는 추가하지 않는다.
- 모니터 안에는 선택한 스위치와 소리 상태 등 필요한 정보만 둔다.
- 공식 스토어 배지는 원형을 보존하고, 3D 장난감이나 전환 오브젝트로 취급하지 않는다.

### 도파민대학교

- AI 심리테스트 플랫폼이라는 정체성이 짧은 한글 설명과 실제 테스트 이름으로 전달되어야 한다.
- 실제 제공하는 테스트의 이미지와 이동 경로를 사용한다. 임의의 예시 퀴즈·분석 결과를 만들지 않는다.
- 테스트 탐색과 토끼의 반응을 연결할 수 있지만, 테스트를 시작하는 데 토끼 조작을 요구하지 않는다.
- 테스트 목록의 최종 배열과 영문 주요 카피는 홈의 디자인 체계를 선택한 뒤 결정한다.

### 홈 아래 장면·푸터·404 (2차 추가, 미확정)

- 홈에는 스튜디오 수준 정보만 둔다. 제품 지표(다운로드·평점·출시 국가)는 해당 제품 페이지에만.
- 한 페이지 한 강조색. 홈에서 도파민대학교의 코랄은 로고·이미지 안에만 있고 UI 색으로 쓰지 않는다.
- 벤토 그리드와 거대 워드마크 푸터는 사용자 검토에서 제외(2026-09-13). 제품 영역은 장면 두 개로 구성한다: Typer 장면(민트 틴트 띠, 실제 모니터·키보드·케이블 데스크, 스위치 7종 알약으로 진짜 타건음 재생, 공식 스토어 배지)과 도파민대학교 장면(중립 배경, 실제 테스트 커버 두 장을 기울여 겹친 카드 더미, 호버 시 앞으로). 그 아래 스튜디오·연락 한 줄. 푸터는 헤어라인 위 한 줄(잉크 워드마크 + 저작권, 링크, 잠든 토끼 2D).
- 상단바 좌측 브랜드는 잉크 워드마크 텍스트 로고(2026-09-13).
- 404 페이지를 추가한다: 토끼 + 한 줄 + 홈으로 가는 링크. GitHub Pages의 `404.html`.

## 11. 최적화 계획과 검증 기준

아래 항목은 구현 시 지킬 목표이며, 현재 성능이 달성되었다는 뜻이 아니다.

### 구조 원칙

- 주요 카피·내비게이션·제품 링크는 3D 초기화와 무관하게 먼저 표시한다.
- 지속하는 3D 공간과 렌더러는 하나로 운용하는 방향을 우선 검토한다. 섹션마다 토끼와 렌더러를 복제하지 않는다.
- 포인터·스크롤·직접 조작·잔여 복귀 동작이 있을 때만 프레임을 갱신한다. 동작이 끝나면 연속 렌더링을 멈춘다.
- 화면 밖, 숨겨진 탭, 페이지 이탈 시 렌더링과 관련 이벤트·리소스를 정리한다.
- 모델, 재질, 조명 환경, 반복되는 글자 형상을 공유한다. 반복적인 생성·폐기를 피한다.
- 과도한 실시간 그림자, 후처리, 물리 엔진, 고해상도 환경 이미지를 기본안에 넣지 않는다.
- 투명 재질의 렌더링 비용을 별도로 측정한다. 기본 재질과 같은 비용이라고 가정하지 않는다.
- WebGL을 사용할 수 없거나 사용자가 움직임을 줄인 경우를 위한 정적 브랜드 구성과 일반 탐색을 설계한다. 오류는 기록하고, 실패를 정상적인 3D 작동으로 보고하지 않는다.

### 초기 성능 예산 — 측정 후 조정

| 항목 | 목표 시작점 |
|---|---|
| 첫 3D 표시까지 추가 전송량 | 압축 기준 600KB 이내 검토 |
| 토끼 + 팀 이름 + 기본 액세서리 | 약 20,000 triangles 이내 검토 |
| 렌더링 해상도 | 데스크톱 DPR 최대 1.5, 모바일 최대 1.0부터 검토 |
| 활동 중 프레임 | 데스크톱 60fps, 모바일 30fps를 목표로 측정 |
| 정지 상태 | 지속적인 프레임 렌더링 없음 |
| 첫 콘텐츠 표시 | 3D 로딩으로 카피·링크가 늦어지지 않음 |

### 완료 전에 확인할 것

- 정면·측면·움직이는 상태에서 원래 토끼로 인식되는지.
- 여덟 글자가 항상 `NewMeans` 순서로 읽히는지.
- 어떤 재질에서도 토끼·팀 이름·카피의 색 대비가 유지되는지.
- 별도 설명 없이 첫 방문자가 조작 가능한 요소를 발견하는지.
- 조작을 하지 않아도 제품과 이동 경로가 이해되는지.
- 첫 화면과 다음 장면에서 동일한 토끼 상태와 클릭 반응이 유지되는지.
- 모바일 터치, 키보드, 움직임 줄이기 설정에서도 사용할 수 있는지.
- 초기 전송량, 최대 해상도, 활동 중 프레임 시간, 정지·비가시 상태의 프레임 수를 실제로 측정했는지.
- 재질을 여러 번 바꾸고 장면을 왕복해도 모델·텍스처·리스너가 누적되지 않는지.

## 12. 협의 순서와 구현 시작 조건

1. **정지 상태의 디자인 결정**: 같은 구도로 폰트 2안, 카피 후보, 기본 팔레트를 비교한다. 토끼 아래의 3D 팀 이름까지 포함한다.
2. **3D 아트 방향 결정**: 토끼와 개별 글자의 정면·측면, 두께, 모서리, 기본 재질을 확인한다.
3. **행동 규칙 결정**: 카피 → 토끼 → 글자의 반응 순서, 발견 가능한 호버·포커스 상태, 작은 움직임의 정도를 합의한다.
4. **전환과 제품 배치 결정**: 같은 토끼가 이어지는 방식, 제품 영역에서의 위치, 모바일 재배치를 정한다.
5. **사용자가 구현 시작을 요청하면 적용**: 합의한 한 방향만 실제 사이트에 구현하고 성능·접근성·반응형 검증을 진행한다.

현재 다음 협의의 우선순위는 폰트 1안/2안, 주요 카피, 기본 재질이다. 문서의 모든 미확정 항목을 한 번에 묻거나, 답변 전 전체 페이지를 다시 구현하지 않는다.
