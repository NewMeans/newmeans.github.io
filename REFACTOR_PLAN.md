# NewMeans 리팩토링 계획 v2: 히어로 아래 전부

작성: 2026-09-15. 기준 브랜치 `dev/refine-product-experience`(작업 트리 포함). 근거 문서: `DESIGN_SYSTEM.md`(토큰·규칙), `DESIGN_PLAN.md` §7·§10·§11, `.claude/product-marketing.md`(사실), `.claude/skills/newmeans-copy/SKILL.md`(보이스), 사용자가 지정한 안티슬롭 프론트엔드 스킬(`design-taste-frontend`). 외부 레퍼런스의 사이트명·캡처는 `_local/refs/`에만 둔다(§0 규칙).

상태: 계획. 사용자 확인 뒤 §8 순서로 구현.

## 0. 결론 한 줄

첫 화면(히어로)만 남기고 그 아래를 전부 다시 짠다. 홈 = 히어로 + Typer 장면(포물선으로 튀는 공이 키캡을 눌러 글자를 친다) + 도파민대학교 2열 카드(콩이 말풍선 위를 튀며 읽고, 보고서가 채워진다) + 한 줄 푸터. 제품 페이지 둘은 각각 5섹션 원페이지. Typer 페이지 첫 화면은 가운데 책상 + 좌우 원형 부품 칩의 커스터마이저. 스크롤 연출은 GSAP ScrollTrigger 하나로 통일하고 직접 스크롤 리스너는 없앤다.

유지: 히어로 전체(`hero.js`, `home.css` 히어로 부분), `keyboard.js`, `i18n.js` 구조, 디자인 토큰, `product.js`의 보드(벽돌깨기)·도형 필드·기울임 카드, 도파민 페이지의 "2,000개 도형" 아이디어, main 브랜치의 책상 구도, 이 브랜치 커밋 840b48f의 케이블 규칙(`--plug-y`).

폐기: 홈의 두 고정 장면(현재 구현), 하단 엔드 카드, Typer 페이지 현재 레이아웃 전부, 도파민 페이지의 3등분 카드·채팅 실험실 히어로, main의 3문항 미니 테스트, 제품 페이지의 3열 푸터.

## 1. 현재 상태 감사 (2026-09-15 캡처, `_local/notes/shots-2026-09-15-audit/`)

| 위치 | 증상 | 원인 | 처리 |
|---|---|---|---|
| 홈 Typer 장면 | 공이 꺾은선으로 움직이고 키캡 숫자(3→2→1)의 변화에 의미가 없다. 로고 아래 빈 공간이 크다 | 궤적이 7개 점의 선형 보간(`product.js` `PATH`). 숫자는 게임 HP를 흉내만 냄. 진행률 계산이 `addEventListener('scroll')`(시스템 §5 위반) | 재작성(§4.2) |
| 홈 도파민 장면 | 스크롤을 끝까지 내려도 로고와 힌트만 보이고 보고서 카드가 나타나지 않는다 | `.report`가 자기 자신에 `--p:0`을 선언해(`product.css:67`) 장면이 넣는 `--p`를 가린다. 결과적으로 opacity가 항상 0 | 폐기, 2열 카드로(§4.3) |
| 홈 하단 | 검정 대형 토끼 + 워드마크 + 링크 한 줄. 페이지 끝의 의도가 안 읽힌다 | 콜로폰 실험 | 폐기, 새 푸터(§4.4) |
| 홈 모바일 | 히어로 중간이 비고, 장면은 정적 프레임 한 장 | 장면 CSS가 720px 이하를 "마지막 프레임"으로만 처리 | 새 장면은 모바일 안무를 따로 정의 |
| Typer 페이지 | 모니터 화면 텍스트가 모니터 밖에 찍히고, 선인장이 거대하고, 케이블이 따로 떠 있고, "아이템 4종"과 부품 피커가 비어 있다 | 작업 트리의 `product.js`에서 `mountDesk`·`mountChat`·아이템 마운트가 삭제됐고 `product.css`의 `.desk` 규칙도 사라졌는데, `typer.html`은 여전히 `stage.__deskApi`를 기다린다(`typer.html:205`, 없으면 즉시 return). 커밋 840b48f까지는 존재 | 페이지 전체 재작성(§5). 케이블 규칙만 840b48f에서 되살림 |
| 도파민 페이지 | 히어로 오른쪽이 빈칸. 3등분 동일 카드. 커버 2장, 도형 필드, 3열 푸터 | `[data-chat-lab]` 마운트가 작업 트리에 없음. 3등분 카드는 스킬 §9.C 금지 패턴 | 재작성(§6), 도형 필드 유지 |
| main 브랜치 | 책상(모니터 + 선인장 + 키보드 + 케이블, CRT에 코드 타이핑)은 좋다. 미니 테스트는 내용이 약하다. 피커는 정사각 썸네일 4줄 | | 책상 구도만 가져온다 |

공통: 페이지 셋이 서로 다른 푸터를 갖고 있고, i18n 키에 쓰지 않는 항목이 20개 이상 남아 있다.

## 2. 조사 요약

### 2.1 이론에서 가져온 규칙

1. 스크롤텔링의 기본: 사용자가 속도를 정한다. 그래서 고정 장면(pin)의 움직임은 스크롤바에 묶고(scrub), 등장(reveal)은 한 번만 한다(once). 되감기는 고정 장면 안에서만 자연스럽다.
2. 한 비트에 한 생각. 뷰포트 하나에 오브젝트 하나, 카피 한 줄, 행동 하나. 현재 홈 장면은 이 원칙을 따르고 있었고, 실패는 원칙이 아니라 구현이다.
3. 첫 화면의 무게가 LCP를 결정한다. 히어로는 지금처럼 정적 HTML + 지연 스크립트, 라이브러리는 히어로 아래에서만 쓴다.
4. 물리: 2차 베지어 곡선은 정확히 포물선이다. 공의 궤적을 SVG `Q` 세그먼트로 이으면 진짜 포물선이 되고, 앵커(키캡 윗면)와 제어점(정점)만 정하면 된다.
5. 스킬 규칙 중 이번 작업에 직접 걸리는 것: 3등분 동일 카드 금지, 한 페이지에서 같은 레이아웃 가족 반복 금지(8섹션이면 4가족 이상), 아이브로 없음, 모션은 한 문장으로 동기를 말할 수 있어야 함, 스크롤 리스너 금지, 마퀴 최대 1개, reduced-motion 필수, 같은 의도의 CTA 라벨 하나, 줄표 금지.
6. 디자인 시스템 유지: 한 페이지 한 강조색, 표면의 90%는 중립, 라운드 4종(알약/20/12/8), 홈에 제품 숫자 없음, 그림자 대신 헤어라인.

### 2.2 레퍼런스에서 뽑은 패턴 (사이트명은 `_local/refs/notes.md`)

1. 커스터마이저: 단색 풀블리드 배경, 가운데 큰 오브젝트, 좌우에 부품별 원형 칩 묶음(묶음당 4~8개), 클릭 즉시 교체, 제목 + 안내 한 줄. 사용자가 지정한 페이지의 구조를 그대로 Typer 책상에 옮긴다.
2. 고정 장면 + scrub: 제품 하나 = 뷰포트 하나. 오브젝트가 스크롤로 동작을 끝까지 보여준다.
3. 등장 한 번 카드: 뷰포트 75~80% 지점에서 올라오고, 다시 올려도 자리를 지킨다. 여러 장은 배치(batch)로 스태거.
4. 제품 원페이지의 리듬: 인터랙티브 첫 화면 → 동작 설명 → 증거(숫자) → 화면 → CTA. 섹션마다 레이아웃 가족을 바꾼다.
5. 푸터는 한두 줄. 거대 워드마크형 푸터는 이 브랜드의 조용한 톤과 맞지 않는다.

## 3. 기술 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 스크롤 엔진 | GSAP 3.13 이상 + ScrollTrigger + MotionPathPlugin. `vendor/gsap/3.13.0/`에 자체 호스팅(gzip 약 40KB). 히어로 아래 스크립트에서만 로드(`defer`) | 2025년부터 전 플러그인 무료. 고정·스크럽·한 번 등장·경로 이동을 한 API로. 빌드 없이 `<script>` 3개 |
| CSS scroll-driven animations | 채택 안 함 | 2026-09 기준 Firefox 안정판이 플래그 뒤. 폴백을 두 벌 만들 이유가 없다 |
| 진행 규칙 | 고정 장면 = `scrub: 0.6`. 카드 등장 = `once: true`. 카드 안 연출 = 래칫(진행률이 줄지 않게 `onUpdate`에서 `max`를 취함) | "이미 등장한 카드는 다시 올려도 그 자리" 요구를 그대로 코드 규칙으로 |
| 반응형·접근성 | `ScrollTrigger.matchMedia`. 720px 이하와 `prefers-reduced-motion`은 고정 없이 마지막 프레임 정적 | 시스템 §5, 스킬 §6.B |
| 소리 | 첫 포인터 입력 이후에만 재생(현행 `touched` 게이트 유지) | 자동재생 정책 |
| 파일 | `scenes.js`(홈 장면 2개) 신설, `configurator.js`(Typer 커스터마이저) 신설, `product.js`는 보드·도형 필드·기울임만 남김, `home.css`의 장면 블록·`typer.css`·`dopamine.css` 재작성, `base.css`에 공용 푸터 | 페이지별 스크립트가 공용 파일을 기다리다 깨지는 현재 구조를 끊는다 |
| 삭제 | `product.js`의 `mountScene`·`mountReport`·`PATH`·`addEventListener('scroll')`, `home.css`의 `.scene*`·`.stage--*`·`.end*`, i18n 미사용 키 | |

## 4. 홈

### 4.1 구조

```text
hero (유지, 100dvh)
#typer  고정 장면, 220vh, 배경 m-100
#dopa   2열 카드, 플로우, 배경 c-100, 위아래 120px
footer  한 줄
```

내비는 지금처럼 `nav--wide`. `제품` 링크는 `#typer`, `스튜디오` 링크는 푸터(`#studio`)로.

### 4.2 Typer 장면: 포물선과 타이핑

무대 760×360 단위. 키캡 5개가 바닥에 x = 100·240·380·520·660으로 선다(키캡 폭 104). 위쪽에 한 줄짜리 "타이핑 줄"(픽셀 서체, 커서 깜빡임)이 있다.

공의 경로는 하나의 SVG 경로다. 시작점 왼쪽 밖 (-40, 40) → 키캡1 윗면 (100, 252) → 키캡2 → ... → 키캡5 → 오른쪽 밖 (800, 20). 인접한 앵커 사이는 전부 `Q`(2차 베지어) 하나, 제어점은 두 앵커 중간 x에 y = 앵커보다 150~190 위. 첫 구간과 마지막 구간만 조금 높게. MotionPathPlugin이 이 경로를 따라 공을 옮기고(ease none), 앵커에 닿는 진행률이 곧 타임라인 위치가 된다.

| 진행률 | 일어나는 일 |
|---|---|
| 0.00~0.12 | 로고 1.08 → 1.0. 키캡 5개가 아래에서 올라온다(스태거 0.02) |
| 0.12~0.82 | 공이 포물선 5개를 그린다. 앵커 진행률 0.22·0.37·0.52·0.67·0.82 |
| 앵커마다 | 키캡 4px 눌림(90ms) → 타건음(첫 입력 이후) → 키캡 위 글자가 나타나고, 타이핑 줄에 같은 글자가 찍힌다 |
| 0.82~0.90 | 마지막 키캡이 부서진다(파편 4개, 현행 CSS 재사용). 타이핑 줄에 커서만 남는다 |
| 0.86~1.00 | 힌트 한 줄 → 스토어 배지 2개 + 텍스트 링크 1개 |

글자에 의미를 두는 방식. 두 안 중 하나를 고른다.

- A안 타이핑(권장): 키캡은 빈 캡으로 시작하고, 공이 닿을 때마다 T·Y·P·E·R이 한 글자씩 새겨지며 위 타이핑 줄에 `TYPER▌`가 완성된다. "공이 키보드를 친다"가 곧 제품 이름이 된다. 구현이 단순하고 EN/KO 공통.
- C안 게임 그대로: 키캡에 게임처럼 HP 숫자(3·2·1)가 있고, 닿을 때마다 하나씩 줄어 0이 되면 캡이 부서지며 아래 글자가 드러난다. 다섯 글자가 T·Y·P·E·R. 게임 규칙에 충실하지만 스크롤 길이가 늘고(한 캡에 여러 번 닿아야 함) 첫 방문자에게는 숫자의 뜻이 안 읽힌다.

포인터: 키캡에 올리면 눌리고 누르면 타건음(현행). 공은 만질 수 없다.

720px 이하·reduced-motion: 고정 없음. 타이핑이 끝난 마지막 프레임(다섯 글자 다 찍힘, 공 없음, 마지막 캡 부서짐) + 힌트 + 배지를 세로로 쌓는다.

### 4.3 도파민대학교 장면: 2열 카드와 튀는 콩

플로우 섹션(고정 없음). 폭 최대 1040px, `grid-template-columns: 1fr 1fr`, 간격 24. 위에 로고, 아래 힌트 + 버튼.

- 카드 1 "카카오톡 대화": 말풍선 4개(현행 i18n 문장 재사용), 오른쪽 위에 `dodae.me` 표기 없음. 배경 `--bg`, 라운드 20, 헤어라인.
- 카드 2 "연구 보고서": 제목 줄, 회색 줄 5개, 테토·에겐 축과 바늘, 도장. 현행 `.report` 마크업을 CSS 변수 의존 없이 다시 쓴다.
- 콩: 도파민 심볼(코랄, 28px). 카드 1 왼쪽 위 밖에서 들어와 말풍선 1 → 2 → 3 → 4 위를 포물선으로 튄다(§4.2와 같은 `Q` 경로 방식). 닿을 때마다 말풍선이 3px 눌리고 태그 칩(직진·공감·ㅋㅋ·질문)이 붙는다.

순서와 트리거.

| 순서 | 트리거 | 방식 |
|---|---|---|
| 로고 페이드 | 섹션 상단이 뷰포트 75% | once |
| 카드 1 등장 (y 40 → 0) | 카드 1 상단이 뷰포트 78% | once |
| 콩이 말풍선 4개를 튄다 | 카드 1 상단 78% → 30% 사이 진행률 | 래칫 |
| 카드 2 등장 | 콩이 4번째 말풍선에 닿는 순간 | once |
| 보고서 줄이 그려지고 바늘이 태그 수만큼 기운다, 도장 | 카드 2 상단 78% → 30% | 래칫 |
| 힌트 + 버튼 | 카드 2 등장 후 0.3초 | once |

포인터: 말풍선을 누르면 태그가 바뀌고 바늘이 다시 계산된다(현행 규칙). 720px 이하: 1열, 카드 1 → 카드 2 순서 그대로, 콩 연출은 유지하되 경로를 짧게(위→아래 직선에 가까운 포물선 4개). reduced-motion: 태그와 보고서가 완성된 상태로 정적.

### 4.4 푸터 (하단바 신설)

헤어라인 하나 위에 두 줄. 어두운 면 없음, 대형 아트 없음.

```text
──────────────────────────────────────────────────────────────
NewMeans(잉크 워드마크 16px)   Typer   도파민대학교   Instagram   개인정보 처리방침        메일 보내기
© 2026 NewMeans · Seoul, Korea                                                 게임과 AI 서비스, 두 사람
```

- 두 번째 줄 오른쪽 문구는 `#studio` 앵커의 내용이자 홈의 유일한 스튜디오 설명이다(명사형, 숫자 없음). 없어도 되면 뺀다.
- 제품 페이지 셋이 같은 마크업을 쓴다. 인스타그램은 Typer 계정이므로 라벨을 `Instagram`으로 유지.
- 잠든 토끼는 쓰지 않는다. 히어로 토끼 하나로 충분하다.
- 720px 이하: 링크 줄바꿈 허용, 두 줄 → 세 줄.

### 4.5 카피 (홈, `i18n.js`)

| 키 | KO | EN |
|---|---|---|
| `home.typer.hint` | 키캡이 부서질 때마다 타건음 | A switch sound for every keycap that breaks |
| `home.typer.more` | 키보드 골라보기 | Build your keyboard |
| `home.dopa.card1` | 카카오톡 대화 | KakaoTalk chat |
| `home.dopa.card2` | 연구 보고서 | Research report |
| `home.dopa.hint` | 내 카톡으로 보는 나의 다른 모습 | Another side of you, read from your chats |
| `home.dopa.more` | 테스트 시작하기 | Start the test |
| `foot.line` | 게임과 AI 서비스, 두 사람 | Games and AI services, two people |

`home.dopa.more`는 도파민 페이지의 CTA와 같은 라벨로 통일한다(CTA 의도 하나 = 라벨 하나).

## 5. Typer 페이지 (원페이지)

섹션 5개 + 푸터. 레이아웃 가족이 전부 다르다.

| # | id | 내용 | 가족 |
|---|---|---|---|
| 1 | `#build` | 커스터마이저(첫 화면) | 가운데 오브젝트 + 좌우 칩 |
| 2 | `#play` | 코어 루프 3단계(왼쪽 목록) + 폰 프레임 안의 진짜 보드(오른쪽, `product.js` 보드) | 분할 |
| 3 | `#numbers` | 30,000+ · 4.74★ · 177 · 15와 "전부 오가닉, 유료 마케팅 0원" | 틴트 밴드, 숫자 4개 |
| 4 | `#screens` | 스크린샷 3장(가운데 올림) | 이미지 행 |
| 5 | `#get` | 스토어 배지 2개, 한 줄 | 중앙 CTA |

### 5.1 커스터마이저 레이아웃

```text
┌────────────────────────────────────────────────────────────────────┐
│ NewMeans / Typer                        #play  #build     KO·EN    │
│                                                                    │
│                 Typer 워드마크 + 한 줄 (키캡을 부수는 손맛과 타건음)   │
│                                                                    │
│  키캡            ┌──────────────────────────┐          모니터      │
│  ○ ○ ○ ○         │        [모니터 화면]       │          ○ ○ ○ ○    │
│  ○ ○ ○ ○         │   TYPER                  │                     │
│                  │   > keycap  Retro        │          스위치      │
│  프레임          │   > frame   Retro        │          ○ ○ ○ ○    │
│  ○ ○ ○ ○         │   > switch  Blue  ▌      │          ○ ○ ○      │
│  ○ ○ ○ ○     선인장└──────────────────────────┘ 케이블              │
│                  [           키보드           ]                    │
│                                                                    │
│                 아무 키나 눌러 타건음 듣기 · App Store · Google Play  │
└────────────────────────────────────────────────────────────────────┘
```

- 그리드: `minmax(200px,1fr) minmax(440px,600px) minmax(200px,1fr)`, 높이 `calc(100dvh - 64px)` 최소, 배경 `--m-100` 풀블리드(홈 Typer 장면과 같은 색이라 이어진다).
- 칩: 지름 56px 원, `--surface` 배경, 헤어라인, 안에 `_s.png` 썸네일(에셋 있음: 키캡 8, 프레임 8, 모니터 4, 스위치 7). 선택 = `--accent` 2px 링 + 이름 툴팁 아래 13px. 묶음 제목은 13px 라벨 하나(아이브로 아님, 그냥 그룹 이름). 왼쪽 = 키캡·프레임(각 8, 4×2), 오른쪽 = 모니터(4)·스위치(7, 4+3).
- 책상: main 브랜치 구도 그대로. `.desk{width:min(100%,560px)}`, 모니터 76%, 선인장 왼쪽 아래 18%, 키보드 `keyboard.js`, 케이블은 840b48f의 규칙(`left:88%;top:3%;width:90%;transform:translate(-4%,calc(-1 * var(--plug-y)))`, 프레임별 `anchorY`)을 그대로 되살린다. 모니터별 `aspect-ratio`와 화면 `inset`은 현행 `MONITORS` 데이터(유니티 실측) 사용.
- 조작: 칩 클릭 → 즉시 교체 + 타건음 1회. 키보드 키 → 타건음 + 모니터 마지막 줄에 글자 타이핑. 스위치 칩 → 소리만 바뀌고 모니터 `switch` 줄이 다시 타이핑된다.
- 조합 프리셋(현행 `THEMES` 4개)은 칩 위에 작은 텍스트 링크 4개로 남긴다. 탭 UI는 없앤다.
- 900px 이하: 책상이 위, 칩 묶음 4개가 아래에 가로 스크롤 스냅 행으로. 칩 48px.

### 5.2 모니터 화면 텍스트

- 영역: 모니터별 `inset`(2000: `11% 9% 22% 9%`, 2001·2002: `4% 4% 24% 4%`, 2003: `6% 6% 33% 6%`), 안쪽 패딩 6%, `overflow:hidden`.
- 서체: DNF BitBit, 크기 = 모니터 폭의 2.8%(`cqw`), 행간 1.4, 최대 6줄, 줄당 22자 이내, 줄바꿈 없음.
- 내용(고정 4줄 + 입력 1줄):

```text
TYPER
> keycap   Retro
> frame    Retro
> monitor  Retro
> switch   Blue
▌
```

- 부품을 바꾸면 해당 줄만 지워졌다가 다시 타이핑된다(글자당 28ms, 글자마다 타건음은 아니고 시작에 한 번). 키를 누르면 마지막 줄에 눌린 키의 글자(키캡 위치 → QWERTY 6×3 대응)가 찍히고 22자를 넘으면 지워진다.
- 색은 모니터 `fg`(2000 `#65CDA7`, 2001 `#E2E2E2`, 2002 `#FFAEEC`, 2003 `#73B77B`). 대비가 낮은 2001·2002는 화면 배경을 `#1E1E1E`로 고정해 3:1 이상 확보.

### 5.3 카피 (Typer)

| 키 | KO | EN |
|---|---|---|
| `typer.tagline` | 키캡을 부수는 손맛과 타건음 | Smash keycaps. Hear every hit. |
| `typer.build.title` | 내 책상 만들기 | Build your desk |
| `typer.build.hint` | 아무 키나 눌러 타건음 듣기 | Press any key to hear the switch |
| `typer.play.title` | 드래그로 조준, 손 떼면 발사 | Drag to aim, release to fire |
| `typer.num.note` | 전부 오가닉, 유료 마케팅 0원 | All organic, zero paid marketing |
| `typer.get.title` | App Store와 Google Play에서 무료 | Free on the App Store and Google Play |

대안(헤드라인): "부서지는 키캡, 진짜 스위치 소리" (사물 나열형, 조금 더 건조). 아이템 4종 섹션은 원페이지에서 뺀다(보드 안에서 보이므로 설명 불필요).

## 6. 도파민대학교 페이지 (원페이지)

강조색 코랄(`body[data-accent="coral"]`). 섹션 5개 + 푸터.

| # | id | 내용 | 가족 |
|---|---|---|---|
| 1 | `#top` | 왼쪽 로고·헤드라인·한 줄·CTA, 오른쪽 홈과 같은 2열 카드 조각을 세로로(카드 1 위, 카드 2 아래, 콩 연출 그대로) | 분할 |
| 2 | `#how` | 한 행 흐름: 대화 내보내기 → dodae.me에 올리기 → 연구 보고서. 아이콘 + 한 줄, 화살표로 연결. 카드 아님 | 가로 흐름 |
| 3 | `#tests` | 테스트 커버 2장(기존 이미지, 기울임 유지) | 2열 이미지 |
| 4 | `#unique` | 풀블리드 도형 필드. 커서로 젓기(현행) + 한 번 누르면 가까운 도형이 커지며 "이 도형은 하나뿐" 라벨 | 풀블리드 캔버스 |
| 5 | `#get` | CTA 하나 + 준비물 한 줄 | 중앙 CTA |

도형 필드 개선: 현재는 카드 안에 갇힌 점무늬로 읽힌다. 섹션 폭 전체로 펼치고 도형을 지금보다 크게(최소 6px), 색을 코랄 램프 3단(400·300·700)으로 줄여 소음을 낮춘다. 제목은 필드 위에 겹치지 않고 위에 둔다.

### 6.1 카피 (도파민대학교)

헤드라인 후보. 하나를 고른다.

1. `내 카톡에는 어떤 내가 있을까?` (현행, 질문형. 방문자 상황을 그대로 묻는다)
2. `대화에 남아 있는 나` (명사형. 가장 조용하고, 로고 옆에서 짧다)
3. `말투가 말해주는 나` (명사형. "AI가 말투를 읽는다"는 기능을 담는다)

| 키 | KO | EN |
|---|---|---|
| `dopa.lead` | 카카오톡 대화 하나로 AI가 쓰는 연구 보고서. 정해진 유형 없이, 2,000명이면 2,000개의 결과. | One chat export, one AI research report. No fixed types: 2,000 people, 2,000 results. |
| `dopa.cta` | 테스트 시작하기 | Start the test |
| `dopa.how.title` | 카톡 대화 하나로 시작 | One chat export is all it takes |
| `dopa.how.1` | 카카오톡에서 대화 내보내기 | Export a KakaoTalk chat |
| `dopa.how.2` | dodae.me에 올리기 | Upload it at dodae.me |
| `dopa.how.3` | 연구 보고서 받기 | Get the research report |
| `dopa.tests.title` | 지금 할 수 있는 테스트 | Tests you can take now |
| `dopa.unique.title` | 정해진 유형 없음 | No fixed types |
| `dopa.unique.sub` | 2,000명이면 2,000개의 결과. 이 2,000개 도형도 같은 것이 없음. | 2,000 people, 2,000 results. None of these 2,000 shapes repeat either. |
| `dopa.get.sub` | 카카오톡 대화 파일 하나면 준비 끝 | One KakaoTalk chat file is all you need |

CTA는 페이지 전체에서 `테스트 시작하기` 하나. `dodae.me에서 열기`는 삭제. 캐릭터 소개(테토·에겐 교수)는 보고서 카드의 축 라벨로만 등장한다.

## 7. 공용 변경

- 푸터 마크업 통일(§4.4). `base.css`에 `.foot` 재정의, `home.css`의 `.end*` 삭제, `typer.css`·`dopamine.css`의 푸터 규칙 삭제.
- 내비: 제품 페이지 링크는 2개(Typer: `#play`·`#build`, 도파민: `#how`·`#tests`). 한 줄, 64px 유지.
- `i18n.js`: §4.5·§5.3·§6.1 키 추가, 미사용 키 삭제(`home.typer.kind/desc/sub`, `typer.kit`, `dopa.flow`, `typer.desk.*`, `typer.item.*`, `typer.loop.*`, `typer.items.*`, `typer.custom.*` 중 미사용, `typer.story`, `home.dopa.lab/kind/desc/sub`, `dopa.report`, `home.studio.lead`, `home.keyboard.hint`, `foot.say/products/contact/sleep`, `sound.*`, `typer.try`, `dopa.hero.t1/t2`, `dopa.lead` 구버전, `dopa.foot.sub`, `dopa.test*.label`). 삭제 전 `grep -n "키"`로 참조 0 확인.
- `product.js`: `mountBoard`·`mountTilt`·`mountField`·`Sound`만 남긴다. `mountPlay`는 Typer `#play`에서 폰 프레임 안 보드로 재사용.
- `README.md`의 구조 목록과 `WORK_PLAN.md` 상태 줄 갱신. `DESIGN_SYSTEM.md` §4 "제품 장면" 항목을 §4.2·§4.3 결정으로 교체.

## 8. 작업 순서

각 단계 끝에 캡처(1440×900, 390×844)와 콘솔 오류 0을 확인하고 커밋한다. 단계 3까지가 홈, 4~5가 제품 페이지, 6이 마무리.

| 단계 | 작업 | 파일 | 완료 조건 |
|---|---|---|---|
| 0 | GSAP 자체 호스팅, `scenes.js`·`configurator.js` 뼈대, `product.js`에서 장면·보고서·스크롤 리스너 제거, 푸터 마크업 통일 | `vendor/gsap/`, `scenes.js`, `configurator.js`, `product.js`, `base.css`, `index.html`, `typer.html`, `dopamine.html` | 세 페이지 모두 콘솔 오류 0, 스크롤 리스너 0(`grep "addEventListener('scroll'"` 결과 없음) |
| 1 | 홈 Typer 장면(§4.2). 경로 SVG, 타임라인, 키캡 글자, 소리, 모바일·reduced-motion 분기 | `index.html`, `home.css`, `scenes.js`, `i18n.js` | 앵커 5개에서 눌림·글자·소리가 맞아떨어짐. 스크롤 되감기 시 역재생 매끄러움. 390px에서 마지막 프레임 |
| 2 | 홈 도파민 장면(§4.3). 카드 2장, 콩 경로, 래칫, 보고서 | 같음 | 위로 올려도 카드가 남음. 태그 클릭 시 바늘 갱신. 390px 1열 |
| 3 | 푸터(§4.4)·내비 앵커·홈 카피(§4.5)·미사용 CSS 삭제 | `home.css`, `base.css`, `i18n.js` | 스킬 §14 체크 통과(홈). 커밋 |
| 4 | Typer 페이지(§5). 커스터마이저 → 보드 섹션 → 숫자 → 화면 → CTA | `typer.html`, `typer.css`, `configurator.js` | 부품 27개 전부 교체 확인, 케이블이 8개 프레임 모두에서 포트에 꽂힘, 모니터 4종에서 텍스트가 화면 안, 900px 이하 가로 스크롤 칩 |
| 5 | 도파민 페이지(§6). 히어로 조각 재사용, 흐름 행, 커버, 필드, CTA | `dopamine.html`, `dopamine.css`, `product.js`(필드) | 도형 필드 풀블리드·클릭 반응, 헤드라인 확정, CTA 라벨 하나 |
| 6 | 카피 최종(KO/EN 대조), 문서 갱신(§7), 접근성·성능 측정, PR | `i18n.js`, `README.md`, `WORK_PLAN.md`, `DESIGN_SYSTEM.md` | §9 전부 체크 |

## 9. 검증 체크리스트

- 콘솔 오류 0. 스크롤 리스너 0. 정지 상태에서 `requestAnimationFrame` 호출 0(보드가 멈춘 뒤).
- 홈: 1440×900·1280×720·390×844·844×390 캡처. 히어로 변화 없음(픽셀 비교). Typer 장면 앵커 5개 타이밍. 도파민 카드 되감기 유지.
- Typer 페이지: 27개 부품 교체 캡처(키캡 8, 프레임 8, 모니터 4, 스위치 7). 케이블 8종. 모니터 텍스트 4종. 900px·480px 칩 행.
- 도파민 페이지: 헤드라인 1·2·3 캡처 비교 후 확정. 필드 2,000개에서 프레임 60fps(데스크톱).
- 스킬 §14: 줄표 0, 아이브로 0, 3등분 카드 0, 같은 레이아웃 가족 반복 0, CTA 의도별 라벨 1, 마퀴 0, 스크롤 안내 문구 0, 대비 AA, reduced-motion에서 정적.
- 디자인 시스템: 한 페이지 한 강조색, 라운드 4종 외 값 없음, 홈에 숫자 없음, 어두운 면은 기본 버튼뿐.
- Lighthouse 모바일: LCP 2.5s 이하, CLS 0.1 이하. GSAP는 히어로 아래에서만 로드됐는지 네트워크 탭 확인.

## 10. 사용자 결정이 필요한 항목

1. Typer 장면 글자 방식: A안 타이핑(권장) / C안 게임 HP.
2. 도파민 장면: 플로우 + 래칫(권장) / 고정 장면 유지.
3. 도파민 헤드라인: 1 / 2 / 3.
4. Typer 커스터마이저 배경: `--m-100` 풀블리드(권장, 홈 장면과 이어짐) / 페이퍼.
5. 푸터 둘째 줄의 스튜디오 한 줄 문구: 유지 / 삭제.
6. GSAP: 자체 호스팅(권장) / CDN.

## 11. 위험

- 포물선 앵커와 키캡 위치는 무대 단위(760×360)로 고정되지만 키캡 이미지의 실제 윗면은 PNG 안쪽에 있다. 앵커 y를 이미지 기준으로 한 번 실측해야 공이 "떠서" 튀지 않는다.
- 래칫 방식은 사용자가 빠르게 스크롤해 지나가면 연출이 순간 완료된다. 카드 안 타임라인은 `scrub: 0.4`로 따라오게 하고 최종 상태가 항상 읽히도록 설계한다.
- 모니터 2001·2002의 화면 인셋은 유니티 실측값이지만 웹 이미지 해상도에서 1~2% 오차가 있다. 단계 4에서 4종 모두 캡처로 맞춘다.
- `i18n.js` 미사용 키 삭제는 `hero.js`가 동적으로 조합하는 키(`home.status.*`, `home.words*`, `home.letter.action`, `home.rabbit.action`)를 건드리면 안 된다. 삭제 목록은 §7의 명시 항목으로 제한한다.
- GSAP 파일을 저장소에 넣으면 공개 저장소에 라이선스 파일도 함께 둔다(`vendor/gsap/LICENSE`).
