# NewMeans 리팩토링 계획 v2.9: 히어로 아래 전부

작성: 2026-09-15. 기준 브랜치 `dev/refine-product-experience`, 기준 커밋 b1be4da. 첫 감사는 그 직전 작업 트리에서 했고, 같은 시각 다른 세션이 제품 페이지를 한 열 원페이지로 바꿔 커밋했다. §1은 커밋 뒤 상태다.

개정 이력(2026-09-15):
- 2차: 공은 천장 구멍에서 나와 물리대로 튀다 바닥 구멍으로 사라지고, 다시 보기로 게임의 공 스프라이트가 무작위로 다시 떨어진다. 도파민대학교는 "AI 심리테스트 플랫폼"을 앞세우고 제품마다 태그 2개. CTA는 제품당 최대 2개.
- 3차: 도파민대학교 헤드라인은 "열여섯 유형으로는 담기지 않는다"는 뉘앙스(문장 그대로가 아니라).
- 4차: 게임 프로젝트의 상점 에셋을 그대로 복사해 씀(`assets/shop/catalog.json`). GSAP는 CDN. Typer 커스터마이저 배경은 프레임별 배경색이고 가운데서 원형 마스크로 번지며 바뀐다.
- 5차: 카피 최소화. 홈 장면에는 설명 문장·힌트·예시 텍스트가 없다. 모든 것을 인터랙션으로 추상 표현하고 실제 내용은 제품 페이지에서 본다. 제품 페이지도 필요한 정보만 짧게. AI 말투 금지.
- 12차(2026-09-16): 스튜디오는 문장 대신 애니메이션. 토끼 두 마리가 CEO·CTO 명찰을 달고 달리고, 시련을 뜻하는 물건(바위·가시·번개·벽돌·불꽃·허들)이 오른쪽에서 무작위로 날아와 왼쪽으로 지나간다. 스크롤 속도가 그 속도를 밀어 준다. 무한 반복. 도파민대학교 페이지는 도형 필드가 히어로가 된다: 진한 도형들이 `1000 명의 사람 / 개의 유형`을 직접 쓰고 나머지는 옅게 흩어진다. 숫자 뒤섞기, 테스트 타일 카드, 분홍-파랑 그라데이션 축은 전부 삭제. 카피: `16개 유형으로는 정의할 수 없는 우리를 위해`, `채팅 기록 업로드`, `같은 모양이 하나도 없는 도형들`. 제품 페이지마다 하단 섹션(자기 행동 + 다른 제품으로 가는 카드).
- 11차(2026-09-16 조정): 제품 태그를 로고 위로. Typer 무대의 천장·바닥 띠 삭제, 구멍은 위아래 절반으로 갈라지는 포털(열리기 직전 나타나고 닫힌 뒤 사라짐)이고 공은 그 사이로 드나든다. 공은 굴러간 거리만큼 회전한다. 마지막 키캡은 부서지지 않는다: 치고 튕겨 올라 바닥의 포털로 떨어진다(최종 프레임에 TYPER가 온전히 남는다). 스튜디오 섹션 신설(`#studio`). 도파민 장면은 채팅 말풍선을 버리고 테스트 타일 여섯 개 중 하나를 고르는 연출로. 홈의 연한 민트 틴트 버튼 폐기, 제품 색을 채운 `.btn--brand`로 교체(Typer m-700, 도파민대학교 c-700).
- 10차(구현 시작): 굵은 디스플레이 = Pally(Fontshare), 단 헤더(히어로 헤드라인)에만. Typer 페이지의 나머지 굵은 글자(숫자 4개 등)는 DNF BitBit. 도파민대학교는 깔끔한 서체 위주: 큰 숫자도 SUIT 800, `명의 사람` SUIT 700, `개의 결과`만 Hahmlet 800(구현자 선택). AI 반짝은 마침표 자리(`개의 결과` 끝). 사용자 지시로 더 묻지 않고 §8 구현을 시작한다.
- 9차: 후기는 전문을 쓰지 않고 후기마다 좋은 문장 하나만(App Store 캡처 5개 + Google Play 7개, 목록은 `_local/`에만). 굵은 서체는 Google Fonts 밖 디자이너 파운드리(Collletttivo·Velvetyne·Fontshare)에서 Baloo 2와 같은 무게·온기로 9종을 더 봤다(보드 3). "명의 사람"과 "개의 결과"는 서로 다른 서체로 하고 이 자리에만 쓰는 서체 하나를 더 허용한다. AI 아이콘의 네 꼭짓점 별(안으로 파인 마름모)을 도파민대학교 모티프로 쓴다.
- 8차: 얇은 쪽(한글·UI·본문)은 Wanted Sans 대신 SUIT. 굵은 디스플레이는 후보 12종을 더 보드로 만들었다(보드 2).
- 7차: 서체는 얇은 모노 대신 굵고 지금 히어로와 온기가 비슷한 그로테스크로(후보 보드 6종, 사용자 선택 대기). 숫자 폭은 서체가 아니라 고정폭 칸으로 잡는다. 후기는 사용자가 내려받아 둔 Google Play 후기 CSV(2025-08~2026-01)에서 골랐다.
- 6차: 도파민대학교 헤드라인은 문장이 아니라 숫자다. 거대한 숫자가 계속 뒤섞이고 오른쪽 두 줄에 `명의 사람` / `개의 결과`. 서체를 디자이너 조합(미스매치)으로 바꾼다. Typer 페이지에 실제 후기 칸을 만든다(후기는 사용자가 고른다). "전부 오가닉" 한 줄 삭제. Typer 안내 문장은 글자 없는 신호로 대체.

근거 문서: `DESIGN_SYSTEM.md`, `DESIGN_PLAN.md` §7·§10·§11, `.claude/product-marketing.md`, `.claude/skills/newmeans-copy/SKILL.md`, 사용자가 지정한 안티슬롭 프론트엔드 스킬. 외부 레퍼런스의 사이트명·캡처는 `_local/refs/`에만.

## 0. 결론 한 줄

히어로만 남기고 아래를 전부 다시 짠다. 홈 = 히어로 + Typer 장면(천장 구멍에서 떨어진 공이 키캡 다섯을 물리대로 튀며 T·Y·P·E·R을 새기고 바닥 구멍으로 사라진다) + 도파민대학교 장면(콩이 글자 없는 메시지 모양 위를 튀고, 글자 없는 보고서가 스스로 써진다) + 한 줄 푸터. 장면에 문장은 없다. 로고, 태그 2개, 무대, 버튼 2개뿐. 제품 페이지는 원페이지이고, Typer 첫 화면은 가운데 책상 + 좌우 원형 부품 칩의 커스터마이저(게임 상점의 6개 카테고리 60개 부품 전부). 스크롤 연출은 GSAP ScrollTrigger(CDN) 하나.

유지: 히어로 전체, `keyboard.js`, `i18n.js` 구조, 디자인 토큰, `product.js`의 보드·도형 필드·기울임 카드, 도파민 페이지의 "2,000개 도형" 아이디어, main 브랜치의 책상 구도, 커밋 840b48f의 케이블 규칙(`--plug-y`).

폐기: 홈의 두 고정 장면(현재 구현), 세 페이지의 엔드 카드, b1be4da의 한 열 제품 페이지 구조, main의 3문항 미니 테스트, 도파민대학교 관련 현행 카피 전부, 홈의 힌트 문장·설명 문장·스토어 배지·텍스트 링크 CTA, Typer 페이지의 태그라인("키캡을 부수는 손맛과 타건음")과 코어 루프 3단계 목록.

## 1. 현재 상태 감사 (2026-09-15 캡처, `_local/notes/shots-2026-09-15-audit/`)

| 위치 | 증상 | 원인 | 처리 |
|---|---|---|---|
| 홈 Typer 장면 | 공이 꺾은선으로 움직이고 키캡 숫자 변화에 의미가 없다 | 궤적이 7개 점의 선형 보간. 진행률 계산이 `addEventListener('scroll')` | 재작성(§4.2) |
| 홈 도파민 장면 | 끝까지 내려도 보고서 카드가 나타나지 않는다 | `.report`가 자체 `--p:0`으로 장면의 `--p`를 가린다(`product.css:67`) | 폐기, 추상 2열 카드로(§4.3) |
| 홈 제품 링크 | 13~15px 텍스트 링크라 눈에 띄지 않는다. 스토어 배지가 홈에 있다 | | CTA 규칙(§7) |
| 홈 하단 | 검정 대형 토끼 + 워드마크 | 콜로폰 실험 | 새 푸터(§4.4) |
| 홈 모바일 | 히어로 중간이 비고 장면은 정적 프레임 한 장 | | 모바일 안무 정의 |
| Typer 페이지 (b1be4da) | 한 열: 로고, 한 줄, 배지, 보드, 스위치 7개, 스크린샷 3, 숫자 3, 엔드 카드. 책상도 부품 선택도 없다 | 직전 작업 트리에서 `product.js`의 desk 마운트가 삭제돼 깨져 있었고, b1be4da가 페이지를 통째로 바꿨다 | §5로 교체 |
| 도파민 페이지 (b1be4da) | 한 열: 로고, 한 줄, CTA, 보고서 카드, 커버 2, 도형 필드 | 카피가 "카톡 분석"을 앞세운다. 도형 필드는 카드 안에 갇혔다 | §6으로 교체 |
| 상점 데이터 | 사이트의 스위치 이름이 게임과 다르다(사이트 4003 Chocolate, 게임 4003 White 등). 게임에는 프레임 13, 키캡 15, 스위치 9, 공 10, 오브제 9가 있는데 사이트는 8·8·7·1·1 | 손으로 옮긴 데이터 | `catalog.json`으로 교체(§7) |

공통: 세 페이지가 엔드 카드를 푸터로 쓰고, i18n에 쓰지 않는 키가 20개 이상이다.

## 2. 조사 요약

### 2.1 이론에서 가져온 규칙

1. 사용자가 속도를 정한다. 고정 장면은 scrub, 등장은 once, 되감기는 고정 장면 안에서만.
2. 한 비트에 한 생각. 뷰포트 하나에 오브젝트 하나, 행동 하나. 문장은 없어도 된다.
3. 첫 화면의 무게가 LCP를 정한다. 라이브러리는 히어로 아래에서만.
4. 물리: 등속 x + 시간에 대해 2차인 y = 중력 운동. GSAP에서 x는 `ease: none`, y는 상승 `power1.out`·하강 `power1.in`. 경로 플러그인이 필요 없다.
5. 스킬 규칙: 3등분 동일 카드 금지, 같은 레이아웃 가족 반복 금지, 모션은 동기가 있어야, 스크롤 리스너 금지, reduced-motion 필수, 같은 의도의 CTA 라벨 하나, 줄표 금지. 아이브로 금지는 제품 태그에 한해 예외(§7).
6. 디자인 시스템: 한 페이지 한 강조색, 표면의 90%는 중립, 라운드 4종, 홈에 제품 숫자 없음, 그림자 대신 헤어라인.

### 2.2 레퍼런스에서 뽑은 패턴 (사이트명은 `_local/refs/notes.md`)

1. 커스터마이저: 단색 풀블리드 배경, 가운데 큰 오브젝트, 좌우에 부품별 원형 칩 묶음, 클릭 즉시 교체, 제목 + 안내 한 줄.
2. 고정 장면 + scrub, 끝난 뒤 다시 보기.
3. 등장 한 번 카드: 다시 올려도 자리를 지킨다.
4. 제품 원페이지 리듬: 인터랙티브 첫 화면 → 오브젝트 → 증거(숫자) → 화면 → CTA.
5. 제품 카드의 정보 순서: 태그 → 이름 → 큰 버튼. 작은 텍스트 링크는 클릭되지 않는다.
6. 푸터는 한 줄.

### 2.3 서체 조사 (6차)

- AI 생성 사이트의 서체 신호: Inter가 사실상 기본값이고, Space Grotesk·Plus Jakarta·DM Sans·Manrope·Instrument Serif·Fraunces가 그 뒤를 따른다. Bricolage Grotesque(현재 히어로 디스플레이)는 금지 목록에는 없지만 2023년 이후 "친근한 디스플레이"가 필요할 때 생성 도구가 자주 집는 Google Fonts 중 하나라 티가 난다. 한글·UI·본문은 사용자 결정(8차)으로 SUIT(sunn.us, OFL, 가변 100~900). 생성 도구 기본값(Pretendard·Noto Sans KR)이 아니고 폭이 넉넉해 볼드 디스플레이 옆에서 조용하다. DNF BitBit은 워드마크 전용이라 그대로.
- 디자이너 조합의 공통점: 성격이 다른 두 가족을 일부러 붙인다(그로테스크 + 고대비 세리프, 모노스페이스 + 에디토리얼 세리프). 이 사이트에는 이미 픽셀(격자) 워드마크가 있으므로, 격자 DNA를 공유하는 모노스페이스 세리프를 디스플레이로 두고 한글은 중립 고딕으로 받치는 조합이 가장 자연스럽다.
- 결정 방향(7차): 굵고, 지금 히어로(Bricolage Grotesque 600)와 온기가 비슷한 그로테스크. 얇은 모노스페이스 세리프(Xanh Mono) 안은 폐기. 후보 6종을 같은 세 장면(히어로 헤드라인, 숫자 헤드라인, Typer 숫자)으로 나란히 렌더한 보드를 `_local/drafts/fonts-bold.html`과 아티팩트로 두었다. A Bricolage Grotesque 800(현재 서체, 굵기만) · B Cabinet Grotesk 800(Fontshare) · C Schibsted Grotesk 800 · D Familjen Grotesk 700 · E Wix Madefor Display 800 · F Clash Grotesk 700(Fontshare). 보드 2(`_local/drafts/fonts-bold-2.html`, 아티팩트)에는 12종을 더 놓았다: G Host Grotesk 800 · H Funnel Display 800 · I Parkinsans 800 · J Rethink Sans 800 · K Red Hat Display 800 · L Anybody 800 · M Baloo 2 800(이상 Google Fonts) · N Telma 700 · O Pally 700 · P Ranade 700 · Q Britney 700 · R Chillax 700(이상 Fontshare). 보드 2는 한글 줄·태그·버튼을 SUIT로 렌더한다. 사용자는 지금까지 중 Baloo 2를 가장 좋아했고 Google Fonts 밖에서 더 찾으라고 했다. 보드 3(`_local/drafts/fonts-bold-3.html`, 아티팩트)은 디자이너 파운드리의 무료 서체 9종: S Coconat Bold · T Mattone Black · U Ribes Black · V Apfel Grotezk Satt(이상 Collletttivo, 밀라노) · W Pilowlava · X Bluu Next Bold · Y Sporting Grotesque Bold(이상 Velvetyne, 파리) · Z Pally 700(Fontshare) · 기준 Baloo 2 800. 유료 참고: Gooper, Ohno Softie, PP Pangram Sans Rounded, Bagoss, Cooper Black, Recoleta. 한글·UI·본문 = SUIT, 픽셀 = DNF BitBit(유지).
- 숫자 뒤섞기의 폭은 서체에 기대지 않는다. 자리마다 0.62em 고정폭 칸을 두고 가운데 정렬하면 어떤 서체든 흔들리지 않는다.

## 3. 기술 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 스크롤 엔진 | GSAP 3.13 + ScrollTrigger, CDN 두 줄(`cdn.jsdelivr.net/npm/gsap@3.13.0/dist/gsap.min.js`, `ScrollTrigger.min.js`). 히어로 아래에서만 `defer` | 사용자 결정(4차). 서버·빌드 없음. 폰트도 같은 CDN |
| CSS scroll-driven animations | 채택 안 함 | Firefox 안정판 미지원(2026-09) |
| 진행 규칙 | 고정 장면 = `scrub: 0.6`. 카드 등장 = `once: true`. 카드 안 연출 = 래칫(`onUpdate`에서 `max`). 다시 보기 = `scrollTrigger.disable()` → `restart()` → `progress(1)` + `enable()` | |
| 반응형·접근성 | `ScrollTrigger.matchMedia`. 720px 이하와 reduced-motion은 마지막 프레임 정적. 다시 보기는 시간 재생 | |
| 소리 | 첫 포인터 입력 이후에만. 다시 보기 클릭이 첫 입력 | 자동재생 정책 |
| 상점 데이터 | `assets/shop/catalog.json` 하나(게임 데이터에서 생성, §7). 사이트 JS는 이 파일만 읽는다 | 손으로 옮긴 표 폐기 |
| 파일 | `scenes.js`(홈), `configurator.js`(Typer), `product.js`(보드·도형 필드·기울임), `base.css`(푸터·태그·CTA), `home.css`·`typer.css`·`dopamine.css` 재작성 | |
| 삭제 | `product.js`의 `mountScene`·`mountReport`·`PATH`·스크롤 리스너, `home.css`의 `.scene*`·`.end*`, i18n 미사용 키 | |

## 4. 홈

### 4.1 구조와 텍스트 인벤토리

```text
hero (유지)
#typer   고정 장면 220vh, 배경 m-100
#dopa    2열 카드, 플로우, 배경 c-100
footer   한 줄
```

장면 하나의 구성: 로고 → 태그 2개 → 무대 → 버튼 2개. 문장 없음. 홈에 존재하는 텍스트 전부: 내비(제품·스튜디오·메일 보내기·KO·EN), 히어로(유지), 태그 4개(`Game` `Mobile` `AI 심리테스트` `Web`), 버튼 4개(`자세히 보기` ×2, `다운로드`, `테스트 시작하기`), 푸터 한 줄. 다시 보기 버튼은 아이콘만(접근성 이름은 `다시 보기`). 무대는 `aria-label`로만 설명한다.

### 4.2 Typer 장면

무대 760×420. 띠는 없다. 포털 둘: 들어오는 쪽 (92, 40), 나가는 쪽 (700, 392). 포털은 64×22 타원을 위아래 절반으로 나눈 것이고, 열릴 때 각 절반이 20씩 벌어져 공(지름 34)이 그 사이로 지난다. 평소에는 보이지 않고 열리기 직전에 나타나 닫힌 뒤 사라진다. 키캡 5개는 x = 92·212·332·452·572, 윗면 y = 269.

물리. 공은 위 포털이 갈라진 사이에서 나와 키캡 1로 떨어지고, 2 → 3 → 4 → 5로 튄다(정점 176 → 152 → 132 → 115). 키캡 5를 치면 R이 새겨지고, 공은 다시 튀어 올라(정점 150) 오른쪽 아래 포털로 떨어진다. 키캡은 부서지지 않는다. 최종 프레임에 TYPER 다섯 글자가 온전히 남는다. 각 구간은 x 등속 + y 상승 `power1.out`·하강 `power1.in`이라 궤적이 정확한 포물선이고, 공은 굴러간 거리에 비례해 회전한다(360 / 2πr, r = 17).

| 진행률 | 일어나는 일 |
|---|---|
| 0.00~0.08 | 태그·로고, 키캡 5개가 올라온다 |
| 0.05~0.14 | 위 포털이 나타나 위아래로 갈라진다 |
| 0.11~0.24 | 공이 그 사이에서 나와 키캡 1로 떨어진다. 포털은 0.18~0.24에 닫히고 0.25~0.29에 사라진다 |
| .24 / .36 / .48 / .60 / .72 | 키캡 1~5: 4px 눌림, 타건음, 캡 위에 T·Y·P·E·R |
| 0.72~0.86 | 키캡 5에서 튕겨 올라 아래 포털로 떨어진다. 포털은 0.73에 나타나 0.76에 갈라지고, 0.855에 공이 들어가고, 0.87에 닫혀 0.94에 사라진다 |
| 0.86~1.00 | 버튼 2개(자세히 보기, 다운로드) → 다시 보기 아이콘 |

다시 보기. 아이콘 버튼. ScrollTrigger를 잠시 끄고 같은 타임라인을 3.4초로 재생한 뒤 끝 상태로 돌아와 다시 켠다. 재생마다 카탈로그의 공 10종 중 직전과 다른 것을 무작위로 쓴다. 720px 이하·reduced-motion: 고정 없음, 마지막 프레임(TYPER) + 버튼.

### 4.3 도파민대학교 장면 (추상, 채팅 아님)

글자가 하나도 없는 카드 두 장. 플로우 섹션, 2열, 폭 최대 1040px. 위에 태그와 로고, 아래 버튼 2개.

- 카드 1: 테스트 여섯 개를 3열 2행 타일로. 각 타일에 변 수가 다른 다각형(3~8각형) 하나. 테마별 테스트가 여럿이고 모양이 저마다 다르다는 뜻이다. 채팅 말풍선은 쓰지 않는다(11차: 채팅 서비스가 아니므로).
- 카드 2: 굵은 제목 바 1개 + 가는 바 4개, 축 하나, 둥근 도장(AI 반짝). 글자 없음.
- 반짝: AI 반짝(네 꼭짓점 별, 코랄, 28px). 카드 1 왼쪽 위 밖에서 들어와 타일 1 → 2 → 3 위를 튀고 아래 가운데 타일에 앉는다. 앉은 타일은 코랄로 채워지고 테두리가 생긴다.

| 순서 | 트리거 | 방식 |
|---|---|---|
| 태그·로고 | 섹션 상단이 뷰포트 75% | once |
| 카드 1 등장 | 카드 1 상단이 뷰포트 78% | once |
| 반짝이 타일 위를 튀고 하나를 고른다 | 카드 1 상단 78% → 28% | 래칫 |
| 카드 2 등장 | 반짝이 앉는 순간 | once |
| 바가 그려지고 바늘이 기울고 도장 | 카드 2 상단 82% → 32% | 래칫 |
| 버튼 2개 | 카드 2 등장 후 0.3초 | once |

포인터: 다른 타일을 누르면 반짝이 그 타일로 옮겨 앉고 바늘이 다시 기운다. 720px 이하: 1열. reduced-motion: 완성 상태 정적. 도파민 페이지 히어로에서 그대로 재사용한다.

### 4.4 스튜디오와 푸터

스튜디오(`#studio`): 문장 없음. 잉크 워드마크, 달리기 장면, `메일 보내기` 버튼뿐이다. 장면은 토끼 두 마리(로고 조각 17개로 조립, 민트)가 나란히 달리는 모습이다. 몸이 위아래로 흔들리고 두 발이 번갈아 구르며, 눈과 코를 오른쪽으로 16% 밀고 몸을 3.5도 기울여 앞을 본다. 각 토끼 아래에 `CEO`, `CTO` 명찰(알약, 헤어라인, 살짝 기울임).

오른쪽에서 장애물이 무작위로 들어와 왼쪽으로 지나간다: 바위·가시·번개·벽돌·불꽃·허들 여섯 가지 SVG, 크기 32~66px, 속도 78~152px/s, 0.64~1.66초 간격. 바닥에 붙은 것은 구르고, 26% 확률로 떠서 온다. 토끼 앞 120px에 들어오면 그 토끼가 뛰어 넘는다(-40px, 0.3s + 0.34s). 스크롤 속도가 `ScrollTrigger`의 `getVelocity()`로 전체 속도에 최대 3.2배까지 더해지고 곧 잦아든다. 화면 밖이면 rAF가 멈춘다. `prefers-reduced-motion`에서는 토끼만 서 있고 장애물은 나오지 않는다.

푸터: 헤어라인 위 한 줄. NewMeans(잉크 워드마크 14px) · Typer · 도파민대학교 · Instagram · 개인정보 처리방침 · newmeans.dev@gmail.com · © 2026 NewMeans, Seoul. 세 페이지 같은 마크업.

### 4.5 홈 i18n

| 키 | KO | EN |
|---|---|---|
| `tag.game` / `tag.mobile` | Game / Mobile | Game / Mobile |
| `tag.aitest` / `tag.web` | AI 심리테스트 / Web | AI personality test / Web |
| `cta.more` | 자세히 보기 | Learn more |
| `cta.download` | 다운로드 | Download |
| `cta.start` | 테스트 시작하기 | Start a test |
| `cta.replay` (aria) | 다시 보기 | Replay |
| `scene.typer` (aria) | 공이 키캡을 튀며 Typer를 친다 | A ball bounces across keycaps and types Typer |
| `scene.dopa` (aria) | 메시지가 읽히고 보고서가 써진다 | Messages get read and a report writes itself |

## 5. Typer 페이지

텍스트 인벤토리: 내비, 태그 2개, 부품 묶음 이름 6개(키캡·프레임·모니터·스위치·오브제·볼), 부품 이름(카탈로그), 보드 상태 한 줄(기능 문구), 숫자 4개와 라벨, 후기 4~6개(사용자가 고른 실제 스토어 후기, 원문 그대로)와 출처, 스토어 배지. 태그라인·설명 문단·섹션 제목·안내 문장 없음.

| # | id | 내용 | 가족 |
|---|---|---|---|
| 1 | `#build` | 커스터마이저(첫 화면) | 가운데 오브젝트 + 좌우 칩 |
| 2 | `#play` | 폰 프레임 안의 진짜 보드(`product.js`) + 스위치 행 + 상태 한 줄. 보드의 공은 §5.1에서 고른 공 | 한 열 오브젝트 |
| 3 | `#numbers` | 30,000+ 다운로드 · 4.74★ 평균 평점 · 177 출시 국가 · 15 리뷰 언어. 숫자는 DNF BitBit(10차), 라벨은 SUIT. 그 외 문장 없음 | 숫자 4개 |
| 4 | `#reviews` | 실제 후기 4~6개(§5.4) | 2열 타일 |
| 5 | `#screens` | 스크린샷 3장(가운데 올림) | 이미지 행 |
| 6 | `#get` | 하단 섹션: 스토어 배지 2개 + 도파민대학교로 가는 카드 | 하단 섹션 |

### 5.1 커스터마이저

```text
┌────────────────────────────────────────────────────────────────────┐
│ NewMeans / Typer                                #play  #get  KO·EN │
│                     Typer 워드마크 · [Game] [Mobile]                 │
│  키캡 (15)        ┌──────────────────────────┐        스위치 (9)    │
│  ○○○○○           │        [모니터 화면]       │        ○○○○○         │
│  ○○○○○           │   TYPER                  │        ○○○○          │
│  ○○○○○           │   > keycap  Retro        │        모니터 (4)    │
│                  │   > frame   Retro        │        ○○○○          │
│  프레임 (13)      │   > switch  Standard ▌   │        오브제 (9)    │
│  ○○○○○      오브제└──────────────────────────┘ 케이블   ○○○○○        │
│  ○○○○○           [           키보드           ]        ○○○○          │
│  ○○○                                                  볼 (10)       │
│                  [ 다운로드 ]                          ○○○○○        │
│                                                       ○○○○○        │
└────────────────────────────────────────────────────────────────────┘
```

- 그리드: `minmax(220px,1fr) minmax(440px,600px) minmax(220px,1fr)`, 높이 최소 `calc(100dvh - 64px)`. 왼쪽 = 키캡·프레임, 오른쪽 = 스위치·모니터·오브제·볼. 칩 44px 원, 한 줄 5개, 묶음 이름 13px + 개수.
- 칩: `--surface` 배경, 헤어라인, 안에 카탈로그의 `thumb`. 선택 = `--accent` 2px 링. 이름은 호버·포커스 툴팁과 모니터 화면에만.
- 배경: 프레임의 `bg`(게임 `backGroundColor`). 프레임을 바꾸면 새 색이 책상 중심에서 원형으로 번지며 이전 색을 덮는다. 구현: 배경 레이어 두 장, 새 색 레이어에 `clip-path: circle(0 at 50% 55%)` → `circle(120vmax at 50% 55%)` 700ms `--ease`, 끝나면 레이어 교체. reduced-motion은 즉시 교체. 첫 화면 색은 Retro `#FFFAED`.
- 배경색 13종: Retro `#FFFAED`, Modern `#DDE7F4`, Mocha Latte `#FFF2DE`, Lavender Sky `#E5EAF5`, Sunset Breeze `#D5E0EC`, Mint Choco `#FFF2DE`, Midnight Edge `#E6E6E6`, Silver Paws `#DFE3E8`, Pure White `#E9EEF5`, Deep Black `#E9EEF5`, Lemon Yellow `#FFF2C7`, Cherry Red `#F1DDD5`, Clear Acrylic `#EAEAEA`. 전부 밝은 톤이라 잉크 텍스트 대비는 유지된다. 홈의 `--m-100`과는 다른 색이므로 홈 장면과 이어지는 색은 포기한다(4차 결정).
- 책상: main 브랜치 구도. 케이블은 840b48f의 규칙(`--plug-y`). 3000~3007의 `anchorY`는 현행 값, 3008~3012는 단계 4에서 실측. Midnight Edge(3006)만 케이블 없음.
- 부품 → 보이는 변화: 키캡 → 키보드 18칸(카탈로그 `slots`), 프레임 → 프레임 이미지·케이블·배경색, 모니터 → 모니터 이미지·화면 색, 스위치 → 소리와 모니터 `switch` 줄, 오브제 → 모니터 왼쪽 아래 오브젝트, 볼 → `#play` 보드의 공(첫 화면에서는 모니터 화면 마지막 줄에 `> ball  Tennis Ball`).
- 조작: 칩 클릭 → 즉시 교체 + 타건음 1회. 키보드 키 → 타건음 + 모니터 마지막 줄에 글자. 버튼은 `다운로드` 하나(`#get`).
- 안내는 글자 없이(6차): 커스터마이저가 화면에 들어오고 1.2초 뒤 키캡 세 개가 차례로 스스로 눌리고(소리 없음), 스위치 묶음 옆의 스피커 아이콘(Phosphor SpeakerHigh)이 두 번 부푼다. 아이콘은 그 뒤 음소거 토글로 남는다. 키보드 위에서 커서는 pointer.
- 900px 이하: 책상이 위, 묶음 6개가 아래에 가로 스크롤 스냅 행. 칩 40px.

### 5.2 모니터 화면

- 영역: 모니터별 `inset`(2000 `11% 9% 22% 9%`, 2001·2002 `4% 4% 24% 4%`, 2003 `6% 6% 33% 6%`), 패딩 6%, `overflow:hidden`.
- 서체 DNF BitBit, 크기 = 모니터 폭의 2.8%(`cqw`), 행간 1.4, 최대 7줄, 줄당 22자, 줄바꿈 없음. 색은 카탈로그 `fg`, 배경 `bg`.

```text
TYPER
> keycap   Retro
> frame    Retro
> monitor  Retro
> switch   Standard
> ball     Rubber
▌
```

- 부품을 바꾸면 해당 줄만 지워졌다가 다시 타이핑(글자당 28ms, 시작에 타건음 한 번). 키를 누르면 마지막 줄에 글자(QWERTY 6×3), 22자 넘으면 지움.

### 5.3 Typer i18n

| 키 | KO | EN |
|---|---|---|
| `typer.group.keycap/frame/monitor/switch/sculpture/ball` | 키캡 / 프레임 / 모니터 / 스위치 / 오브제 / 볼 | Keycaps / Frame / Monitor / Switch / Object / Ball |
| `typer.sound` (aria) | 소리 켜기 / 끄기 | Sound on / off |
| `typer.desk.hint` | 아무 키나 누르기, 또는 화면을 끌어서 조준 | Press any key, or drag to aim |
| `typer.num.dl/rating/countries/langs` | 다운로드 / 평균 평점 / 출시 국가 / 리뷰 언어 | Downloads / Average rating / Countries / Review languages |
| `typer.review.1..8` / `typer.review.1..8.meta` | 후기에서 뽑은 한 문장 / 출처 | 같음 |

메타 제목·설명은 유지("Typer: 기계식 키보드 타건음 아케이드").

### 5.4 후기 (`#reviews`)

규칙(9차): 후기 전문은 쓰지 않는다. 후기마다 좋은 문장 하나만 그대로 뽑는다. 출처: App Store(사용자가 보낸 캡처 2장, 5문장)와 Google Play 후기 CSV(`_local/reviews/`, 깃 제외, 7문장). 1차 선택 8개(2열 4행)와 예비 4개를 `_local/reviews/selection.md`에 두었다. 후보 목록은 `_local/tools/pick_reviews.py`가 `_local/reviews/candidates.md`로 만든다. 후기 원문·후보·선택 목록은 어느 것도 저장소에 올리지 않는다. 사용자가 승인한 6개만 구현 단계에서 `i18n.js`에 들어간다(공개 사이트에 보이는 문장이므로 그때 저장소에 포함된다). App Store 후기는 이 버킷에 없다.

- 형태: 2열 타일(`--surface`, 라운드 20, 패딩 22), 후기 16px SUIT, 출처 13px `--text-2`(`Google Play · 언어`), 다른 언어는 원문 아래 한글 번역 13px. 이름·아바타·별점 없음. 타일당 한 문장. 8개면 4행. 720px 이하 1열.
- 마크업은 i18n 키 `typer.review.1..8`, `.meta`, `.ko`(번역, 선택)로 두고 빈 키는 렌더하지 않는다.
- 새 후기가 쌓이면 CSV를 `_local/reviews/`에 넣고 스크립트를 다시 돌린다.

## 6. 도파민대학교 페이지

텍스트 인벤토리: 내비, 태그 2개, 헤드라인(도형이 직접 쓴다), 도형 캡션 1줄, 사실 1줄, CTA, 단계 명사 3개, 테스트 이름 2개와 한 줄, 하단 CTA와 다른 제품 카드. 그 외 문장 없음.

| # | id | 내용 | 가족 |
|---|---|---|---|
| 1 | `#top` | 태그 → 로고 → 도형 필드 헤드라인 → 캡션 → 사실 1줄 → CTA | 풀블리드 캔버스 히어로 |
| 2 | `#how` | 한 행: 테스트 고르기 → 채팅 기록 업로드 → AI 연구 보고서 | 가로 흐름 |
| 3 | `#tests` | 지금 열린 테스트 2장 + 한 줄 | 2열 이미지 |
| 4 | `#get` | CTA + 다른 제품(Typer) 카드 | 하단 섹션 |

### 6.1 도형이 쓰는 헤드라인

필드가 곧 헤드라인이다. `1000`(SUIT 800), `명의 사람`(SUIT 700), `개의 유형`(Hahmlet 800)을 안 보이는 캔버스에 그려 알파를 마스크로 쓰고, 그 안에 들어가는 도형은 진한 코랄(c-700·c-800·c-600), 바깥은 옅은 코랄(c-200·c-100·c-300)로 칠한다. 글자 안쪽은 촘촘하게(격자 그대로, 흔들림 14%), 바깥은 넷 중 하나만 남겨 흩뿌린다. 도형은 변 수가 3~8로 저마다 다르고 커서로 휘저을 수 있으며, 누르면 가장 가까운 도형이 AI 반짝으로 커진다. 넓은 화면은 숫자 왼쪽 + 두 줄 오른쪽, 좁은 화면은 세로로 쌓는다. 격자 간격은 620px 미만에서 4.8px, 그 위는 6.5~11px. 숫자 뒤섞기는 없다.

| 키 | KO | EN |
|---|---|---|
| `dopa.count.a` / `b` / `sr` | 명의 사람 / 개의 유형 / 사람 수만큼의 유형 | people / types / As many types as there are people |
| `dopa.note` | 같은 모양이 하나도 없는 도형들 | No two of these shapes are alike |
| `dopa.fact` | 16개 유형으로는 정의할 수 없는 우리를 위해 | For all of us that sixteen types cannot describe |
| `dopa.how.1/2/3` | 테스트 고르기 / 채팅 기록 업로드 / AI 연구 보고서 | Pick a test / Upload a chat log / AI research report |
| `dopa.tests.more` | 같은 엔진 위에 새 테스트가 계속 | New tests keep arriving on the same engine |
| `cta.start` | 테스트 시작하기 | Start a test |

## 7. 공용 변경

- 카피 원칙(5차): 홈에는 문장이 없다. 제품 페이지에는 §5·§6의 텍스트 인벤토리에 있는 것만 있다. 새 문장을 넣으려면 인벤토리에 먼저 적는다. 말투는 `.claude/skills/newmeans-copy/SKILL.md` + §6.1 금지 목록.
- 제품 태그: 알약 2개(Typer `Game` `Mobile`, 도파민대학교 `AI 심리테스트` `Web`). 13px, 헤어라인, `--text-2`, 로고 바로 위(11차). `DESIGN_SYSTEM.md` §2 "아이브로 없음"의 유일한 예외.
- CTA 규칙: 제품당 최대 2개. 홈 = 기본 버튼 `자세히 보기`(48px 알약, n-800) + 틴트 버튼 하나(Typer `다운로드` → `typer.html#get`, 도파민 `테스트 시작하기` → dodae.me). 제품 페이지 = 주 행동 하나. 텍스트 링크 CTA와 홈의 스토어 배지는 없다.
- 상점 데이터(4차, 완료): 게임 프로젝트 `Assets/Resources/_ShopItem`을 읽기 전용으로 복사했다. `assets/shop/catalog.json`에 카테고리 6개 60개 부품(공 10, 오브제 9, 모니터 4, 프레임 13, 스위치 9, 키캡 15)의 id·이름·가격·스프라이트·썸네일·색(프레임 배경, 모니터 화면 bg/fg, 키캡 베드·글자·불빛, 공 궤적)·키캡 18칸 `slots`·케이블 유무를 넣었다. 스프라이트 명명은 기존 규칙(`<id>_<n>.png`, `<id>_s.png`). 새 스위치 4007·4008의 소리는 wav를 m4a로 변환해 `assets/audio/switch/`에 넣었다. 사이트의 옛 이름 표(스위치 4000 Blue, 4003 Chocolate 등)는 게임 이름(4000 Standard, 4003 White, 4004 Chocolate, 4007 Blue, 4008 Purple)으로 바꾼다.
- 서체 v2(10차 확정): `--font-display` = Pally 700(Fontshare ITF 무료 라이선스, `assets/fonts/Pally-700.woff2`, 라이선스 동봉). 쓰는 자리는 헤더뿐: 히어로 헤드라인. Typer의 다른 굵은 글자(숫자 4개, 부품 묶음 개수)는 `--font-pixel`(DNF BitBit). 도파민대학교에서는 Pally를 쓰지 않는다(SUIT + Hahmlet만). `--font-ui` = SUIT(`assets/fonts/SUIT-Variable.woff2` 자체 호스팅, OFL 동봉. Wanted Sans CDN 선언과 메트릭 오버라이드 삭제). `--font-pixel` = DNF BitBit(유지). `--font-result` = Hahmlet 800 부분집합(`개의결과results` 글리프만, OFL, `assets/fonts/Hahmlet-800-result.woff2`). Bricolage Grotesque 파일·`bricolage.css`·preload 삭제. 히어로 `.display`는 Pally 700, 크기 그대로. `DESIGN_SYSTEM.md` §2 표 갱신.
- 푸터 통일(§4.4). `base.css`에 `.foot`·`.tag`·`.cta-row`, `home.css`의 `.end*` 삭제.
- 내비: 제품 페이지 링크 2개(Typer `#play`·`#get`, 도파민 `#how`·`#tests`).
- `i18n.js`: §4.5·§5.3·§6.1 키만 남기고 미사용 키 삭제. `hero.js`가 동적으로 쓰는 키(`home.status.*`, `home.words*`, `home.letter.action`, `home.rabbit.action`)는 유지.
- `product.js`: `mountBoard`(공 스프라이트 옵션 추가)·`mountTilt`·`mountField`·`Sound`(스위치 id별 클립 수는 카탈로그에서)만.
- `README.md`·`WORK_PLAN.md`·`DESIGN_SYSTEM.md` §2·§4 갱신.

## 8. 작업 순서

진행(2026-09-16): 0~5단계 구현 완료(커밋 참조). 6단계 중 문서 갱신 완료, Lighthouse·접근성 측정은 남음.

| 단계 | 작업 | 파일 | 완료 조건 |
|---|---|---|---|
| 0 | GSAP CDN 2줄, `scenes.js`·`configurator.js` 뼈대, `product.js` 정리, 푸터·태그·CTA 공용 스타일, 카탈로그 로더 | `index.html`, `typer.html`, `dopamine.html`, `scenes.js`, `configurator.js`, `product.js`, `base.css` | 세 페이지 콘솔 오류 0, 스크롤 리스너 0, 카탈로그 60개 로드 |
| 1 | 홈 Typer 장면(§4.2) | `index.html`, `home.css`, `scenes.js`, `i18n.js` | 앵커 5개 타이밍, 구멍 열림·닫힘, 다시 보기 5회에서 공이 매번 바뀌고 끝난 뒤 스크롤 동기화, 390px |
| 2 | 홈 도파민 장면(§4.3) | 같음 | 글자 0. 위로 올려도 카드가 남음. 바 클릭 시 콩 이동. 390px 1열 |
| 3 | 서체 v2(고른 서체 자체 호스팅, 히어로 굵기 800), 푸터·태그·CTA·홈 i18n·미사용 CSS 삭제 | `base.css`, `home.css`, `i18n.js`, `assets/fonts/` | 히어로가 두 줄 안에 들어감(1280·1440), 홈 문장 0(내비·히어로 제외), 스토어 배지 0, 제품당 버튼 2개. 커밋 |
| 4 | Typer 페이지(§5): 커스터마이저(60개 칩, 배경 원형 전환, 글자 없는 안내) → 보드 → 숫자 → 후기 칸 → 화면 → 배지 | `typer.html`, `typer.css`, `configurator.js`, `i18n.js` | 60개 부품 교체, 케이블 12종 포트 정합, 배경 13색 전환, 모니터 4종 텍스트 정합, 후기 6칸(빈 칸 숨김), 900px 칩 행 |
| 5 | 도파민 페이지(§6): 숫자 헤드라인(뒤섞기), 흐름 행, 커버, 필드, CTA | `dopamine.html`, `dopamine.css`, `product.js`, `i18n.js` | 텍스트 인벤토리와 일치, 숫자 폭 흔들림 0, reduced-motion에서 2000 고정, 필드 풀블리드 |
| 6 | KO/EN 대조, 문서 갱신, 성능·접근성 측정, PR | `i18n.js`, `README.md`, `WORK_PLAN.md`, `DESIGN_SYSTEM.md` | §9 전부 |

## 9. 검증 체크리스트

- 콘솔 오류 0. 스크롤 리스너 0. 정지 상태 `requestAnimationFrame` 0.
- 텍스트 인벤토리: 페이지의 보이는 문자열을 전부 뽑아 §4.1·§5·§6 목록과 대조. 목록에 없는 문장은 삭제.
- 홈: 1440×900·1280×720·390×844 캡처. 히어로 픽셀 변화 없음. 앵커·구멍 타이밍. 다시 보기 5회. 카드 되감기 유지.
- Typer: 부품 60개 교체 캡처, 케이블 12종, 배경 13색 원형 전환, 모니터 4종, 900px·480px.
- 도파민: 헤드라인 1·2·3 비교 후 확정. 필드 60fps. 금지어 검색 결과 0.
- 스킬 §14와 디자인 시스템 규칙(§2.1의 5·6).
- Lighthouse 모바일 LCP 2.5s 이하, CLS 0.1 이하. GSAP는 히어로 아래에서만 로드.

## 10. 결정 상태

결정됨: 후기는 한 문장씩(9차), AI 반짝 모티프(9차), Typer 장면 물리·구멍·다시 보기·공 무작위(2차), 태그와 CTA 규칙(2차), 헤드라인 뉘앙스(3차), 게임 에셋 복사와 카탈로그(4차, 완료), GSAP CDN(4차), 프레임별 배경색 + 원형 마스크 전환(4차), 카피 최소화와 추상 무대(5차), 푸터의 스튜디오 문장 삭제(5차), 도파민 장면 카드 = 추상(5차), 도파민 헤드라인 = 숫자 뒤섞기(6차), "전부 오가닉" 삭제(6차), Typer 안내 문장 삭제 + 글자 없는 신호(6차), Typer 후기 칸(6차, 내용은 사용자 전달 대기).

결정됨(10차): 굵은 디스플레이 = Pally 700(헤더에만). 얇은 쪽 = SUIT. Typer의 다른 굵은 글자 = DNF BitBit. 도파민대학교 = SUIT 중심, `개의 결과`만 Hahmlet 800. AI 반짝 = 마침표 자리. 후기 = `_local/reviews/selection.md`의 8문장으로 구현하고 i18n 키로 교체 가능하게 둔다.

남은 결정: 없음. 사용자 지시로 구현 중에는 묻지 않고 진행하며, 판단이 필요한 부분은 이 문서 §10에 적는다.

## 11. 위험

- Fontshare 서체(B·F)는 ITF 무료 라이선스로 자체 호스팅이 허용되지만 재배포 조건을 `assets/fonts/`의 라이선스 파일로 함께 둔다. Google Fonts 후보는 OFL.
- 후기는 사용자 원문이라 맞춤법·느낌표를 고치지 않는다. 카피 규칙(느낌표 금지)의 예외.
- 숫자 뒤섞기는 70ms 타이머라 탭이 뒤로 가면 멈춰야 한다(`visibilitychange`, IntersectionObserver).
- 배경 13색은 전부 밝은 톤이지만 Lemon Yellow(`#FFF2C7`)와 Mocha Latte(`#FFF2DE`) 위의 `--text-2` 대비는 4.5:1 근처다. 칩 라벨은 `--text-1`로 둔다.
- 새 프레임 3008~3012의 케이블 `anchorY`는 실측 전이다. 카탈로그의 `cablePos`(게임 좌표)를 참고해 단계 4에서 5개를 맞춘다.
- 원형 마스크 전환은 `clip-path` 애니메이션이라 GPU 합성이 필요하다. 레이어에 `will-change: clip-path`를 전환 중에만 준다.
- 다시 보기 재생 중 스크롤은 재생이 끝난 뒤 동기화된다. 재생 중 재클릭은 처음부터 다시(공 교체 포함).
- 포물선 앵커 y와 구멍 타원은 키캡·띠 이미지 기준으로 한 번 실측한다.
- 래칫은 빠른 스크롤에서 연출이 순간 완료된다. `scrub: 0.4`로 따라오게 한다.
- i18n 삭제는 `hero.js` 동적 키를 건드리지 않는다.
- 게임 프로젝트는 읽기 전용이다. 에셋 갱신은 복사 스크립트로만 하고 원본 폴더에는 아무것도 쓰지 않는다.
