# NewMeans 디자인 시스템 v1

상태: 2026-09-12 v1.1. 사용자 피드백 반영: 중립 램프를 밝게, 버튼 텍스트 수직 정렬 보정, 아이브로 없음.
적용 근거: `DESIGN_PLAN.md`(요구사항), `.claude/product-marketing.md`(사실), 사용자가 지정한 안티슬롭 프론트엔드 스킬(`design-taste-frontend`)의 규칙. 외부 레퍼런스 사이트명은 기록하지 않는다.
생성: `_local/tools/palette.py`(램프·대비) → `_local/tools/gen_system.py`(토큰·문서·시안). 값을 바꿀 때는 스크립트를 고치고 다시 생성한다.

## 0. 디자인 읽기와 다이얼

읽기: 2인 게임·AI 스튜디오의 홈과 제품 소개. 대상은 제품 사용자와 협업 파트너(일반 소비자 감각). 언어는 부드럽고 장난기 있는 픽셀 브랜드. 모드는 리디자인 - 시각 전면 교체, 콘텐츠·IA·브랜드(민트, 토끼, 픽셀 워드마크, 명사형 카피)는 보존.

| 다이얼 | 값 | 근거 |
|---|---|---|
| DESIGN_VARIANCE | 7 | 카피 좌상단 / 토끼 우하단의 대각선 비대칭. 제품 영역은 셀 수가 정확한 벤토 |
| MOTION_INTENSITY | 6 | 움직임은 포인터·클릭에 반응하는 3D 상호작용이 담당. 스크롤 하이재킹·상시 루프 없음 |
| VISUAL_DENSITY | 3 | 여백이 첫 화면의 중심. 홈에는 스튜디오 수준 정보만 |

기존 사이트 감사: 브랜드 토큰은 민트 `#85C6A8`, 코랄 `#FF6666`, 페이퍼 `#FAFAF7`, 잉크 `#1B1815`, Wanted Sans, 4–8px 라운드, 게임 UI 전용 DNF BitBit. IA는 `index.html` → `typer.html` / `dopamine.html`, 푸터 연락처, 스토어·인스타 리디렉트. 보존: 슬러그, 내비 라벨, 워드마크, 카피 보이스, 키보드 컴포넌트. 폐기: 잉크 워드마크 히어로, 원색 전면 배경 타일, 어두운 지표 타일, 홈의 제품 지표, 문서형 세리프 한글 제목, 아이브로.

## 1. 색

### 1.1 램프 (OKLCH 등간격, 브랜드 원색이 400 앵커)

| 계열 | 50 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 |
|---|---|---|---|---|---|---|---|---|---|---|
| 중립 n | `#FCFBF7` | `#F9F5F1` | `#F1ECE6` | `#DFDAD4` | `#BCB6B0` | `#97918A` | `#706B64` | `#514D48` | `#3B3732` | `#24211D` |
| 민트 m | `#E9FCF2` | `#DAF7E8` | `#C3EDD8` | `#A8E0C5` | `#85C6A8` | `#6BAB8E` | `#538F74` | `#3E765E` | `#305D4A` | `#234234` |
| 코랄 c | `#FEF4F3` | `#FEE9E7` | `#FFD6D3` | `#FFBEBA` | `#FF6666` | `#F26A68` | `#CE5151` | `#AF3B3D` | `#8B2E2F` | `#622322` |

중립은 잉크와 같은 웜 색상(hue 74)으로 통일하고, 밝은 쪽(100–300)은 채도를 낮춰 베이지로 기울지 않게 한다. 순검정·순백은 쓰지 않는다.

### 1.2 역할 토큰

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--bg` | n-50 `#FCFBF7` | 페이지 배경. 모든 페이지 동일 |
| `--surface` | n-100 `#F9F5F1` | 타일·카드·입력 배경. 흰색 카드 대신 이것 |
| `--surface-hover` | n-200 `#F1ECE6` | 표면 호버 |
| `--line` / `--line-strong` | n-200 / n-300 | 헤어라인. 그림자 대신 |
| `--text-1` | n-800 `#3B3732` | 본문·UI·제목 |
| `--text-2` | n-600 `#706B64` | 보조 설명·캡션(13px 이상) |
| `--text-display` | n-700 `#514D48` | 40px 이상 디스플레이만. 클수록 잉크를 연하게 |
| `--accent` | m-700 `#3E765E` | 강조 텍스트·링크·강조어·아이콘 |
| `--accent-strong` | m-800 `#305D4A` | 틴트 표면 위 작은 텍스트 |
| `--accent-soft` | m-100 `#DAF7E8` | 제품 타일 등 틴트 표면 |
| `--accent-soft-2` | m-200 `#C3EDD8` | 틴트 표면 호버, 틴트 버튼 배경 |
| `--brand` | m-400 `#85C6A8` | 로고·토끼·워드마크·일러스트·2–3px 장식선. 텍스트 금지 |
| `--focus` | m-600 `#538F74` | 포커스 링 2px |
| 버튼 | n-800 위 n-50 | 기본 버튼 하나. 페이지에서 유일한 어두운 면 |

도파민대학교 페이지(`body[data-accent="coral"]`)에서만 `--accent` 계열이 코랄 램프로 바뀐다(c-700 / c-800 / c-100 / c-200 / c-400).

### 1.3 규칙

1. 한 페이지 한 강조색. 홈과 Typer는 민트, 도파민대학교 페이지는 코랄. 홈에서 코랄은 제품 로고·이미지 안에만 존재하고 UI에는 쓰지 않는다.
2. 원색(400)은 그래픽 전용. 텍스트, 텍스트가 놓이는 배경, 버튼에 쓰지 않는다. 텍스트 강조는 700, 틴트는 100·200.
3. 표면의 90%는 중립. 틴트 표면은 한 화면에 한 종류. 틴트 위에는 같은 계열의 700·800 텍스트 또는 잉크만.
4. 어두운 면은 기본 버튼 하나뿐. 어두운 섹션·타일로 페이지 테마를 뒤집지 않는다.
5. 그림자 대신 헤어라인. 그림자가 꼭 필요하면 배경 색상으로 물들인 아주 옅은 그림자 하나.
9. 배경 장식 없음. 첫 화면 배경은 페이퍼 단색이고 토끼 뒤 글로우, 점 격자, 그라데이션을 두지 않는다. 입체감은 3D 옆면 음영과 접지 그림자만으로. 예외는 단어 상태 하나: cyberpunk(에픽)에서만 `:root[data-theme="cyber"]` 다크 토큰 세트와 네온 글로우·스캔라인을 쓴다(2026-09-14). 정보 대비는 유지.
6. 텍스트 대비는 4.5:1 이상, 24px 이상 큰 텍스트는 3:1 이상. 아래 표 밖의 조합은 쓰기 전에 측정한다.
7. 코랄 원색은 채도가 높아 UI 강조로 쓰지 않는다. 도파민대학교 페이지의 UI 강조는 c-700이고, `#FF6666`는 로고·아트워크에만.
8. 다크 모드는 v1에서 라이트 고정. 토큰이 의미 단위라 뒤집을 준비는 되어 있다.

### 1.4 단어 슬롯 팔레트 (2026-09-13)

첫 화면의 강조어와 토끼만 예외적으로 단어 컨셉의 색을 쓴다(한 페이지 한 강조색 규칙의 유일한 예외, 범위는 강조어 텍스트·토끼·액세서리). 램프와 같은 OKLCH 방식으로 토끼 톤(L .78)과 텍스트 톤(L .50, 페이퍼 대비 4.5:1 이상)을 뽑았다. 워드마크·내비게이션·버튼은 바뀌지 않는다.

| 계열 | 토끼 | 텍스트 | 텍스트 대비 |
|---|---|---|---|
| mint | `#85C6A8` | `#3E765E` | 5.12:1 |
| coral | `#FF6666` | `#984645` | 6.15:1 |
| gold | `#DAB249` | `#7C5F00` | 5.8:1 |
| amber | `#E2AB70` | `#885716` | 5.93:1 |
| peach | `#ECA47F` | `#90502C` | 6.02:1 |
| rose | `#EB9DBB` | `#8F4966` | 6.12:1 |
| plum | `#DCA0D6` | `#834D7E` | 6.09:1 |
| lavender | `#C1A9EE` | `#6C5594` | 6.03:1 |
| periwinkle | `#A4B3F8` | `#535D9C` | 5.94:1 |
| sky | `#79C0F1` | `#1F6A96` | 5.7:1 |
| teal | `#5DCBD1` | `#007276` | 5.53:1 |
| sage | `#95C78A` | `#427138` | 5.56:1 |
| olive | `#B5BF73` | `#62691B` | 5.7:1 |
| cocoa | `#D0B198` | `#795D46` | 5.85:1 |
| indigo | `#AAB1F7` | `#595C9B` | 5.91:1 |
| magenta | `#EE95D1` | `#8C4877` | 6.13:1 |
| lime | `#A9C37B` | `#566D27` | 5.61:1 |
| charcoal | `#706B64` | `#3B3732` | 11.4:1 |
| holo | 파스텔 5색 그라데이션 | 진한 5색 그라데이션(각 4.5:1 이상) | 에픽 전용 |

### 1.5 대비 측정

| 조합 | 색 | 대비 | 판정 |
|---|---|---|---|
| 본문 text-1 (n-800) / paper | `#3B3732` / `#FCFBF7` | 11.40:1 | 통과 |
| 보조 text-2 (n-600) / paper | `#706B64` / `#FCFBF7` | 5.10:1 | 통과 |
| 디스플레이 (n-700) / paper | `#514D48` / `#FCFBF7` | 8.10:1 | 통과 |
| 캡션 text-3 (n-500) / paper | `#97918A` / `#FCFBF7` | 3.01:1 | 큰 글자만 |
| 본문 (n-800) / n-100 표면 | `#3B3732` / `#F9F5F1` | 10.89:1 | 통과 |
| 보조 (n-600) / n-100 표면 | `#706B64` / `#F9F5F1` | 4.87:1 | 통과 |
| 민트 텍스트 (m-700) / paper | `#3E765E` / `#FCFBF7` | 5.12:1 | 통과 |
| 민트 텍스트 (m-700) / n-100 | `#3E765E` / `#F9F5F1` | 4.89:1 | 통과 |
| 민트 텍스트 (m-700) / m-100 | `#3E765E` / `#DAF7E8` | 4.66:1 | 통과 |
| 민트 텍스트 (m-800) / m-200 | `#305D4A` / `#C3EDD8` | 5.89:1 | 통과 |
| 본문 (n-800) / m-100 | `#3B3732` / `#DAF7E8` | 10.37:1 | 통과 |
| 코랄 텍스트 (c-700) / paper | `#AF3B3D` / `#FCFBF7` | 5.77:1 | 통과 |
| 코랄 텍스트 (c-700) / c-100 | `#AF3B3D` / `#FEE9E7` | 5.13:1 | 통과 |
| 코랄 텍스트 (c-800) / c-200 | `#8B2E2F` / `#FFD6D3` | 6.25:1 | 통과 |
| 본문 (n-800) / c-100 | `#3B3732` / `#FEE9E7` | 10.13:1 | 통과 |
| paper / n-800 버튼 | `#FCFBF7` / `#3B3732` | 11.40:1 | 통과 |
| paper / m-700 버튼 | `#FCFBF7` / `#3E765E` | 5.12:1 | 통과 |
| paper / c-700 버튼 | `#FCFBF7` / `#AF3B3D` | 5.77:1 | 통과 |
| 원색 m-400 / paper (그래픽) | `#85C6A8` / `#FCFBF7` | 1.91:1 | 텍스트 금지 |
| 원색 c-400 / paper (그래픽) | `#FF6666` / `#FCFBF7` | 2.76:1 | 텍스트 금지 |

## 2. 타이포그래피

| 역할 | 서체 | 크기/행간 | 자간 | 쓰는 곳 |
|---|---|---|---|---|
| display-1 | Bricolage Grotesque 600 | 72–96 / 1.02 | -0.03em | 홈 영문 헤드라인 |
| display-2 | Bricolage Grotesque 600 | 44–56 / 1.05 | -0.025em | 제품 페이지 영문 헤드라인 |
| heading-1 | Wanted Sans 600 | 40 / 1.2 | -0.02em | 한국어 화면 헤드라인 |
| heading-2 | Wanted Sans 600 | 32 / 1.25 | -0.015em | 섹션 제목 |
| heading-3 | Wanted Sans 600 | 24 / 1.3 | -0.01em | 타일 제목 |
| body | Wanted Sans 400 | 16 / 1.6 | 0 | 본문, 최대 65자 |
| body-s | Wanted Sans 400 | 14 / 1.5 | 0 | 보조 설명 |
| label | Wanted Sans 500 | 13–14 / 1 | 0 | 내비, 버튼, 세그먼트, 모서리 |

규칙:
1. 가족은 둘 + 브랜드 하나. 영문 디스플레이 = Bricolage Grotesque, 한국어와 모든 UI·본문 = Wanted Sans, 픽셀(DNF BitBit)은 워드마크에만. 픽셀 서체로 라벨을 만들지 않는다. 예외: 단어 상태 retro·arcade·cyberpunk에서는 첫 화면 헤드라인만 픽셀 서체로 바뀐다(`:root[data-face="pixel"]`, 2026-09-14).
2. 아이브로(제목 위 작은 대문자 라벨)는 쓰지 않는다. 히어로는 헤드라인 + 한 줄 설명, 섹션은 제목만.
3. 한국어 제목에 세리프를 쓰지 않는다. 세리프 디스플레이(Fraunces, Instrument Serif)는 2차 시안에서 검토했으나 스킬 규칙상 기본 선택에서 제외한다.
4. 헤드라인 강조는 같은 가족 안에서 색(`--accent`)으로만. 다른 가족의 단어를 끼워 넣지 않는다.
5. 굵기는 400 / 500 / 600만.
6. 수직 정렬: Wanted Sans는 `ascent-override: 85.5%; descent-override: 14.5%; line-gap-override: 0%`로 선언해 글리프 잉크 중심(베이스라인 위 0.355em)이 콘텐츠 영역 중심과 일치하게 한다. 버튼·세그먼트·내비 링크는 `line-height: 1`에 아래 패딩을 위보다 2px 더 준다(한글 시각 보정). 시안에서는 CDN woff2를 같은 선언으로 감싼 `Wanted Sans Fixed`를 쓰고, 프로덕션은 `assets/fonts/`에 자체 호스팅한다.
7. Bricolage Grotesque에는 이탤릭이 없다. 강조는 색으로.

## 3. 형태·간격·표면

- 라운드 규칙: 상호작용 요소(버튼, 세그먼트, 내비 컨테이너) = 알약. 타일 = 20px. 타일 안의 이미지 = 12px. 입력·작은 칩 = 8px. 이 넷 외의 값은 쓰지 않는다.
- 간격은 4px 단위. 섹션 세로 여백 96 (모바일 64). 타일 패딩 28 (모바일 22). 그리드 간격 16.
- 컨테이너 최대 폭 1240. 히어로 안전 여백은 데스크톱 40, 모바일 22.
- 카드는 계층이 필요할 때만. 그 외는 여백과 헤어라인으로 나눈다.

## 4. 컴포넌트

- 내비게이션: 한 줄, 높이 64. 좌측 브랜드는 잉크 워드마크(높이 16px, `assets/brand/newmeans-wordmark-ink.png`), 중앙 링크(알약 컨테이너 `--surface`, 한국어 화면은 `제품`·`스튜디오`), 우측 언어 세그먼트 + 다시 뽑기 + 기본 버튼 하나. 현재 페이지 링크는 `--text-1`.
- 버튼: 기본 = n-800 배경 알약, 44px. 틴트 = `--accent-soft-2` 배경 + `--accent-strong` 텍스트. 텍스트 링크 = `--accent` + 밑줄. 같은 의도의 CTA는 페이지 전체에서 같은 라벨(연락 = `메일 보내기` 하나).
- 세그먼트(언어): 알약, 헤어라인, 선택 = n-800 채움.
- 타일: `--surface` 기본, 제품 타일만 `--accent-soft`. 제목 heading-3, 라벨 body-s `--accent`, CTA는 타일 하단 정렬.
- 푸터: 헤어라인 위 한 줄. 좌측 잉크 워드마크 + `© 2026 NewMeans, Seoul`, 중앙 링크(Typer, 도파민대학교, Instagram, 개인정보 처리방침), 우측 잠든 토끼(2D 조각, 눈 감김). 거대 워드마크·다열 푸터는 쓰지 않는다.
- 제품 장면: 타일 대신 장면. 제품당 하나의 2열 장면(설명 + 실제 제품 오브젝트). Typer 장면만 `--accent-soft` 띠, 나머지는 페이지 배경. 이미지 위에 라벨을 얹지 않는다. 오브젝트는 제품의 핵심 동작을 그대로 한다(2026-09-15): Typer는 모니터 안 벽돌깨기(실제 키보드 입력 = 발사, 드래그 = 조준, 맞을 때마다 고른 스위치 타건음), 도파민대학교는 스스로 읽히는 채팅(말풍선을 짚으면 태그가 붙고 테토·에겐 보고서가 바뀜, 직접 쓴 문장도 읽힘). 제품 페이지는 같은 조각을 크게 쓴다(`product.js`, `product.css`).
- 아이콘: 손으로 그린 SVG 금지. Phosphor(1.5 stroke) 한 가족으로 통일한다.

## 5. 모션

- 호버 = 표면 한 단계(`--bg`→`--surface`, `--accent-soft`→`--accent-soft-2`). 눌림 = `translateY(1px)`. 전환 200ms `cubic-bezier(.16,1,.3,1)`.
- 상시 반복 애니메이션 없음. 스크롤 리스너 금지: 스크롤 연동은 IntersectionObserver 또는 ScrollTimeline로.
- `prefers-reduced-motion`에서 이동·회전 축소, 상태 변화는 유지 (`DESIGN_PLAN.md` §7).

## 6. 콘텐츠 규칙

- 홈에는 스튜디오 수준 정보만. 제품 지표(다운로드, 평점, 출시 국가)는 해당 제품 페이지에만.
- 카피 보이스는 `.claude/skills/newmeans-copy/SKILL.md`. 명사형 종결, 느낌표·이모지 금지.
- 줄표 금지. 하이픈만 쓴다. 현재 `i18n.js`의 메타 제목에 있는 줄표도 교체 대상.
- 가운뎃점(·)은 한 줄에 하나까지.
- `Scroll` 같은 스크롤 안내 문구 금지. 우측 하단 화살표는 `#works`로 가는 실제 링크이고 접근 가능한 이름을 가진다.
- 벤토 셀 수 = 콘텐츠 수. 빈 셀 없음. 3개 동일 카드 가로 배열 없음. 섹션 제목 옆에 떠 있는 작은 설명 없음.

## 7. 결정 기록

- 색 시스템: 2026-09-12 중립을 밝힌 v1.1로 진행 (사용자 "계속 진행").
- 다크 모드: v1 라이트 고정. 후속 과제.
- 좌하단 `Seoul, Korea`: 사용자 허용. 히어로 모서리 유지.
- 헤드라인 강조어: 같은 서체 안에서 색.
- 아이브로: 없음 (사용자 지시).

## 8. 벤토 초안에서 고친 것

- 원색 전면 배경 타일 두 개 → 틴트(m-100) 하나 + 중립 표면. 코랄은 이미지·로고 안으로.
- 어두운 지표 타일 → 삭제. Typer 지표는 `typer.html`로.
- Noto Serif KR 제목 → Wanted Sans 600.
- 흰색 카드 + 헤어라인 + 그림자 → `--surface` 틴트, 헤어라인만.
- 섹션 제목 우측의 작은 설명 → 삭제.
- 세 가지 라운드 값 혼용 → 알약 / 20 / 12 / 8 규칙.
